<UserControl x:Class="LosasPlus.Views.DiagramView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource BgPrimary}">
    <DockPanel LastChildFill="True">
        <Border DockPanel.Dock="Top" Background="{DynamicResource BgSecondary}" Padding="8" >
            <DockPanel>
                <TextBlock Text="Esquema 2D · scroll = zoom · drag = pan · modo conexión: click 1ra losa → click 2da adyacente"
                           VerticalAlignment="Center"/>
                <Button DockPanel.Dock="Right" Content="Reset zoom" Width="100" Click="OnResetZoom" Margin="6,0,0,0"/>
                <Button DockPanel.Dock="Right" Content="Ajustar" Width="80" Click="OnFitToView" Margin="6,0,0,0"/>
                <Button DockPanel.Dock="Right" Content="Redibujar" Width="100" Click="OnRedraw" Margin="6,0,0,0"/>
                <ToggleButton DockPanel.Dock="Right" x:Name="ConectarModoToggle"
                              Content="Modo conexión" Width="140" Margin="6,0,0,0"
                              Background="{DynamicResource Accent}"
                              Foreground="{DynamicResource FgOnAccent}"
                              Click="OnToggleConectar"
                              ToolTip="Activá este modo, hacé click en dos losas adyacentes y se crea la adyacencia I-J automática (X o Y según la posición relativa)."/>
            </DockPanel>
        </Border>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="240"/>
            </Grid.ColumnDefinitions>

            <!-- Viewport con pan + zoom. Border captura mouse events; el ScaleTransform aplica al canvas hijo. -->
            <Border Grid.Column="0"
                    x:Name="Viewport"
                    Background="#101010"
                    ClipToBounds="True"
                    MouseWheel="OnMouseWheel"
                    MouseLeftButtonDown="OnMouseLeftDown"
                    MouseLeftButtonUp="OnMouseLeftUp"
                    MouseMove="OnMouseMove"
                    Cursor="Cross">
                <Canvas x:Name="Canvas" Width="2000" Height="1500">
                    <Canvas.RenderTransform>
                        <TransformGroup>
                            <ScaleTransform x:Name="Scale" ScaleX="1" ScaleY="1"/>
                            <TranslateTransform x:Name="Translate" X="0" Y="0"/>
                        </TransformGroup>
                    </Canvas.RenderTransform>
                </Canvas>
            </Border>

            <Border Grid.Column="1" Background="{DynamicResource BgSecondary}" Padding="8" Margin="6,0,0,0">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="Controles" FontWeight="Bold"/>
                        <TextBlock Text="• Rueda del mouse: zoom in/out" Margin="0,8,0,0"/>
                        <TextBlock Text="• Click izquierdo + arrastrar: pan"/>
                        <TextBlock Text="• Botón Ajustar: encuadra todo en la vista"/>
                        <TextBlock Text="• Botón Reset zoom: vuelve a escala 1:1"/>

                        <Separator Margin="0,12,0,12"/>
                        <TextBlock Text="Leyenda" FontWeight="Bold"/>
                        <TextBlock Text="Línea verde gruesa = borde compartido / continuidad" Margin="0,8,0,0" TextWrapping="Wrap"/>
                        <TextBlock Text="Línea gris fina = borde simplemente apoyado" TextWrapping="Wrap"/>
                        <TextBlock Text="Línea naranja punteada = balanceo N (voladizo / nudo 3 lados)" TextWrapping="Wrap"/>

                        <Separator Margin="0,12,0,12"/>
                        <TextBlock Text="Etiquetas" FontWeight="Bold"/>
                        <TextBlock Text="• Mfx, Mfy en verde sobre el centro de la losa (vano)" Margin="0,8,0,0" TextWrapping="Wrap"/>
                        <TextBlock Text="• Mu I-J en rojo sobre el borde compartido (apoyo)" TextWrapping="Wrap"/>
                        <TextBlock Text="• Disponer en gris debajo de Mu I-J" TextWrapping="Wrap"/>

                        <Separator Margin="0,12,0,12"/>
                        <TextBlock Text="Estado" FontWeight="Bold"/>
                        <TextBlock x:Name="StatusText" Text="Sin sistema" Margin="0,4,0,0" TextWrapping="Wrap"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </DockPanel>
</UserControl>
