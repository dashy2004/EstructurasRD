# M.2b — Visores 3D (MapLibre + Cesium) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Al exportar GeoJSON, escribir junto al `.geojson` dos visores HTML autocontenidos (MapLibre GL y CesiumJS) que muestran el edificio extruido sobre el mapa, sin servidor ni token.

**Architecture:** Un generador puro `GeneradorVisorMapa` en Core (patrón `ExportadorGeoJson`: estático, sin I/O, string→string) con el GeoJSON embebido inline en cada plantilla HTML (raw string literals C#). El `MainViewModel.ExportarGeoJson()` existente escribe los tres archivos.

**Tech Stack:** .NET 8 (`~/.dotnet/dotnet`), xUnit, C# raw string literals interpoladas (`$$"""`), MapLibre GL 4 (CDN unpkg), CesiumJS 1.119 (CDN cesium.com).

## Global Constraints

- Spec: `docs/superpowers/specs/2026-07-25-m2b-visores-3d-design.md` (aprobado).
- Rama de trabajo: `ui/editor-planta`. Repo: `~/dev/estructurasrd/main`.
- Tests en español con guiones bajos (`Sin_georreferencia_lanza_excepcion`), namespace `LosasPlus.Tests`.
- Excepciones del dominio export: `ExportadorModeloException` (ya existe en `src/Core/Services/ExportadorModeloMotor.cs`).
- Propiedades de extrusión (M.2a, NO cambiar): `base_height` (base del volumen) y `height` (tope absoluto), snake_case.
- Ningún token/clave hardcodeado en las plantillas.
- **SRI obligatorio**: todo `<script>`/`<link>` de CDN lleva versión exacta + `integrity="sha384-..."` + `crossorigin="anonymous"`. Los hashes de las plantillas ya fueron computados y verificados contra los CDN (HTTP 200) el 2026-07-25 — usarlos tal cual; si se cambia de versión, recomputar con `curl -sL <url> | openssl dgst -sha384 -binary | openssl base64 -A`.
- Comando de test: `~/.dotnet/dotnet test tests/LosasPlus.Tests/LosasPlus.Tests.csproj --filter "FullyQualifiedName~GeneradorVisorMapa"` (suite completa sin `--filter` al final).

---

### Task 1: `GeneradorVisorMapa.GenerarMapLibre` + validación + escape

**Files:**
- Create: `src/Core/Services/GeneradorVisorMapa.cs`
- Test: `tests/LosasPlus.Tests/GeneradorVisorMapaTests.cs`

**Interfaces:**
- Consumes: `ExportadorModeloException(string)` de `LosasPlus.Services`.
- Produces: `public static string GenerarMapLibre(string geojson)` — HTML completo; lanza `ExportadorModeloException` si `geojson` es null/vacío. Helper privado `Empotrar(string)` que escapa `</` → `<\/`.

- [ ] **Step 1: Write the failing tests**

```csharp
using LosasPlus.Services;
using Xunit;

namespace LosasPlus.Tests;

/// <summary>
/// Tests del generador de visores 3D (Fase M.2b): HTML autocontenidos
/// (MapLibre GL / CesiumJS) con el GeoJSON extruido de M.2a embebido inline.
/// </summary>
public class GeneradorVisorMapaTests
{
    /// <summary>GeoJSON mínimo con las propiedades de extrusión de M.2a (Santo Domingo).</summary>
    private const string GeojsonDemo = """
        {"type":"FeatureCollection","features":[{"type":"Feature",
         "geometry":{"type":"Polygon","coordinates":[[[-69.94,18.47,25.0],[-69.9399,18.47,25.0],[-69.9399,18.4701,25.0],[-69.94,18.4701,25.0],[-69.94,18.47,25.0]]]},
         "properties":{"tipo":"losa","nivel":"Nivel 1","cota":0.0,"base_height":0.0,"height":3.0}}]}
        """;

    [Fact]
    public void Maplibre_contiene_capa_de_extrusion_y_geojson_embebido()
    {
        string html = GeneradorVisorMapa.GenerarMapLibre(GeojsonDemo);

        Assert.Contains("maplibre-gl", html);              // CDN de la librería
        Assert.Contains("fill-extrusion-base", html);      // consume base_height
        Assert.Contains("fill-extrusion-height", html);    // consume height
        Assert.Contains("base_height", html);
        Assert.Contains("const GEOJSON", html);            // embebido inline
        Assert.Contains("-69.94", html);                   // datos reales dentro
        Assert.Contains("openstreetmap.org", html);        // fondo OSM sin token
        Assert.Contains("integrity=\"sha384-", html);      // SRI en el CDN
        Assert.Contains("crossorigin=\"anonymous\"", html);
    }

    [Fact]
    public void Maplibre_escapa_cierre_de_script_del_geojson()
    {
        string malicioso = GeojsonDemo.Replace("Nivel 1", "a</script>b");

        string html = GeneradorVisorMapa.GenerarMapLibre(malicioso);

        Assert.Contains("a<\\/script>b", html);
        Assert.DoesNotContain("a</script>b", html);
    }

    [Fact]
    public void Maplibre_geojson_nulo_o_vacio_lanza_excepcion()
    {
        Assert.Throws<ExportadorModeloException>(() => GeneradorVisorMapa.GenerarMapLibre(null!));
        Assert.Throws<ExportadorModeloException>(() => GeneradorVisorMapa.GenerarMapLibre("  "));
    }
}
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `~/.dotnet/dotnet test tests/LosasPlus.Tests/LosasPlus.Tests.csproj --filter "FullyQualifiedName~GeneradorVisorMapa"`
Expected: FAIL (compilación: `GeneradorVisorMapa` no existe).

- [ ] **Step 3: Write minimal implementation**

Versión y hashes SRI ya resueltos y verificados (MapLibre GL **4.7.1**, HTTP 200, 2026-07-25) — están puestos en la plantilla de abajo:

```csharp
namespace LosasPlus.Services;

/// <summary>
/// Genera visores HTML <b>autocontenidos</b> del edificio extruido (Fase M.2b):
/// el GeoJSON de <see cref="ExportadorGeoJson"/> va embebido inline (con
/// <c>file://</c> un fetch muere por CORS; inline abre con doble click).
/// MapLibre extruye directo con <c>fill-extrusion</c> desde
/// <c>base_height</c>/<c>height</c> (convención M.2a).
///
/// <para>Función pura, sin I/O — mismo patrón que <see cref="ExportadorGeoJson"/>.</para>
/// </summary>
public static class GeneradorVisorMapa
{
    public static string GenerarMapLibre(string geojson)
    {
        Validar(geojson);
        return $$"""
            <!DOCTYPE html>
            <html lang="es">
            <head>
            <meta charset="utf-8">
            <title>EstructurasRD — visor 3D (MapLibre)</title>
            <script src="https://unpkg.com/maplibre-gl@4.7.1/dist/maplibre-gl.js"
                    integrity="sha384-SYKAG6cglRMN0RVvhNeBY0r3FYKNOJtznwA0v7B5Vp9tr31xAHsZC0DqkQ/pZDmj"
                    crossorigin="anonymous"></script>
            <link href="https://unpkg.com/maplibre-gl@4.7.1/dist/maplibre-gl.css" rel="stylesheet"
                  integrity="sha384-MinO0mNliZ3vwppuPOUnGa+iq619pfMhLVUXfC4LHwSCvF9H+6P/KO4Q7qBOYV5V"
                  crossorigin="anonymous">
            <style>html,body,#mapa{margin:0;height:100%}</style>
            </head>
            <body>
            <div id="mapa"></div>
            <script>
            const GEOJSON = {{Empotrar(geojson)}};
            const coords = GEOJSON.features.flatMap(f => f.geometry.coordinates[0]);
            const lons = coords.map(c => c[0]), lats = coords.map(c => c[1]);
            const map = new maplibregl.Map({
              container: 'mapa',
              style: {
                version: 8,
                sources: { osm: { type: 'raster',
                  tiles: ['https://tile.openstreetmap.org/{z}/{x}/{y}.png'],
                  tileSize: 256, attribution: '© OpenStreetMap' } },
                layers: [{ id: 'osm', type: 'raster', source: 'osm' }]
              },
              bounds: [[Math.min(...lons), Math.min(...lats)], [Math.max(...lons), Math.max(...lats)]],
              fitBoundsOptions: { padding: 120 },
              pitch: 60, bearing: -20
            });
            map.on('load', () => {
              map.addSource('edificio', { type: 'geojson', data: GEOJSON });
              map.addLayer({
                id: 'extrusion', type: 'fill-extrusion', source: 'edificio',
                paint: {
                  'fill-extrusion-base': ['get', 'base_height'],
                  'fill-extrusion-height': ['get', 'height'],
                  'fill-extrusion-color': '#e07b39',
                  'fill-extrusion-opacity': 0.9
                }
              });
            });
            </script>
            </body>
            </html>
            """;
    }

    /// <summary>Escapa <c>&lt;/</c> para que ningún texto del GeoJSON pueda cerrar el script.</summary>
    private static string Empotrar(string geojson) => geojson.Replace("</", "<\\/");

    private static void Validar(string geojson)
    {
        if (string.IsNullOrWhiteSpace(geojson))
            throw new ExportadorModeloException(
                "No hay GeoJSON que embeber en el visor: exporta primero el edificio georreferenciado.");
    }
}
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `~/.dotnet/dotnet test tests/LosasPlus.Tests/LosasPlus.Tests.csproj --filter "FullyQualifiedName~GeneradorVisorMapa"`
Expected: PASS (3 tests).

- [ ] **Step 5: Commit**

```bash
git add src/Core/Services/GeneradorVisorMapa.cs tests/LosasPlus.Tests/GeneradorVisorMapaTests.cs
git commit -m "feat(M.2b): GeneradorVisorMapa — visor MapLibre autocontenido con extrusión"
```

---

### Task 2: `GeneradorVisorMapa.GenerarCesium`

**Files:**
- Modify: `src/Core/Services/GeneradorVisorMapa.cs` (añadir método tras `GenerarMapLibre`)
- Test: `tests/LosasPlus.Tests/GeneradorVisorMapaTests.cs` (añadir tests)

**Interfaces:**
- Consumes: `Empotrar`/`Validar` privados de Task 1 (mismo archivo).
- Produces: `public static string GenerarCesium(string geojson)` — HTML completo; misma excepción en null/vacío.

- [ ] **Step 1: Write the failing tests** (añadir a `GeneradorVisorMapaTests`)

```csharp
    [Fact]
    public void Cesium_contiene_datasource_extrusion_y_geojson_embebido()
    {
        string html = GeneradorVisorMapa.GenerarCesium(GeojsonDemo);

        Assert.Contains("Cesium.js", html);                 // CDN de la librería
        Assert.Contains("GeoJsonDataSource", html);
        Assert.Contains("extrudedHeight", html);            // extrusión por entidad
        Assert.Contains("OpenStreetMapImageryProvider", html); // imagery sin token
        Assert.Contains("const GEOJSON", html);
        Assert.Contains("-69.94", html);
        Assert.Contains("integrity=\"sha384-", html);       // SRI en el CDN
        Assert.Contains("crossorigin=\"anonymous\"", html);
    }

    [Fact]
    public void Cesium_no_lleva_token_hardcodeado()
    {
        string html = GeneradorVisorMapa.GenerarCesium(GeojsonDemo);

        // Los tokens ion son JWT ("eyJ..."); el HTML solo puede leerlo del input.
        Assert.DoesNotContain("eyJ", html);
        Assert.DoesNotContain("defaultAccessToken = \"", html);
    }

    [Fact]
    public void Cesium_escapa_cierre_de_script_y_valida_vacio()
    {
        string malicioso = GeojsonDemo.Replace("Nivel 1", "a</script>b");
        Assert.Contains("a<\\/script>b", GeneradorVisorMapa.GenerarCesium(malicioso));

        Assert.Throws<ExportadorModeloException>(() => GeneradorVisorMapa.GenerarCesium(""));
    }
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `~/.dotnet/dotnet test tests/LosasPlus.Tests/LosasPlus.Tests.csproj --filter "FullyQualifiedName~GeneradorVisorMapa"`
Expected: FAIL (compilación: `GenerarCesium` no existe).

- [ ] **Step 3: Write minimal implementation** (añadir a `GeneradorVisorMapa`)

Hashes SRI de CesiumJS 1.119 ya computados y verificados (HTTP 200, 2026-07-25) — están puestos en la plantilla de abajo:

```csharp
    /// <summary>
    /// Visor CesiumJS: globo con imagery OSM <b>sin token</b>. El campo de token
    /// de Cesium ion es opcional y solo activa el terreno con relieve (World
    /// Terrain) — puerta a 3D Tiles en M.3. La librería CesiumJS es open source
    /// y no lo necesita.
    /// </summary>
    public static string GenerarCesium(string geojson)
    {
        Validar(geojson);
        return $$"""
            <!DOCTYPE html>
            <html lang="es">
            <head>
            <meta charset="utf-8">
            <title>EstructurasRD — visor 3D (Cesium)</title>
            <script src="https://cesium.com/downloads/cesiumjs/releases/1.119/Build/Cesium/Cesium.js"
                    integrity="sha384-19K1W/rKcjyRWzj4BzWH4GVmRVA8OnzNgQ33/vVuXFrRqgZ+Zcm5BNPzyrsYisGU"
                    crossorigin="anonymous"></script>
            <link href="https://cesium.com/downloads/cesiumjs/releases/1.119/Build/Cesium/Widgets/widgets.css" rel="stylesheet"
                  integrity="sha384-ghEeMdcWWzRv/BPeUcX835vcKDGrxvROXisl/Btpv3GeekBUXTSPVcFJpI1Tcrgp"
                  crossorigin="anonymous">
            <style>
            html,body,#globo{margin:0;height:100%}
            #panelToken{position:absolute;top:10px;left:10px;z-index:1;background:#fff;
                        padding:6px 10px;border-radius:4px;font:12px sans-serif}
            </style>
            </head>
            <body>
            <div id="globo"></div>
            <div id="panelToken">
              Token Cesium ion (opcional, activa terreno):
              <input id="token" size="32" placeholder="pegar token...">
              <button onclick="activarTerreno()">Activar</button>
            </div>
            <script>
            const GEOJSON = {{Empotrar(geojson)}};
            const visor = new Cesium.Viewer('globo', {
              baseLayer: new Cesium.ImageryLayer(new Cesium.OpenStreetMapImageryProvider(
                { url: 'https://tile.openstreetmap.org/' })),
              baseLayerPicker: false, geocoder: false, animation: false,
              timeline: false, sceneModePicker: false
            });
            Cesium.GeoJsonDataSource.load(GEOJSON).then(ds => {
              for (const ent of ds.entities.values) {
                const p = ent.properties;
                ent.polygon.height = p.base_height.getValue();
                ent.polygon.extrudedHeight = p.height.getValue();
                ent.polygon.material = Cesium.Color.fromCssColorString('#e07b39');
                ent.polygon.outline = true;
                ent.polygon.outlineColor = Cesium.Color.BLACK;
              }
              visor.dataSources.add(ds);
              visor.flyTo(ds);
            });
            async function activarTerreno() {
              const t = document.getElementById('token').value.trim();
              if (!t) return;
              Cesium.Ion.defaultAccessToken = t;
              visor.terrainProvider = await Cesium.createWorldTerrainAsync();
            }
            </script>
            </body>
            </html>
            """;
    }
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `~/.dotnet/dotnet test tests/LosasPlus.Tests/LosasPlus.Tests.csproj --filter "FullyQualifiedName~GeneradorVisorMapa"`
Expected: PASS (6 tests).

- [ ] **Step 5: Commit**

```bash
git add src/Core/Services/GeneradorVisorMapa.cs tests/LosasPlus.Tests/GeneradorVisorMapaTests.cs
git commit -m "feat(M.2b): visor CesiumJS sin token, con campo opcional de Cesium ion"
```

---

### Task 3: rutas derivadas + integración en `ExportarGeoJson()`

**Files:**
- Modify: `src/Core/Services/GeneradorVisorMapa.cs` (añadir `RutasVisores`)
- Modify: `src/MemoriaPlus/ViewModels/MainViewModel.cs:1093-1100` (método `ExportarGeoJson`)
- Test: `tests/LosasPlus.Tests/GeneradorVisorMapaTests.cs` (añadir test)

**Interfaces:**
- Consumes: `GenerarMapLibre`/`GenerarCesium` (Tasks 1-2); `ExportadorGeoJson.Exportar(Edificio, Georreferencia)` existente.
- Produces: `public static (string MapLibre, string Cesium) RutasVisores(string rutaGeojson)` — solo manipulación de strings (`System.IO.Path`), sin tocar disco.

- [ ] **Step 1: Write the failing test** (añadir a `GeneradorVisorMapaTests`)

```csharp
    [Fact]
    public void Rutas_de_visores_derivan_del_geojson()
    {
        string ruta = System.IO.Path.Combine("salida", "edificio.geojson");

        var (mapLibre, cesium) = GeneradorVisorMapa.RutasVisores(ruta);

        Assert.Equal(System.IO.Path.Combine("salida", "edificio-visor-maplibre.html"), mapLibre);
        Assert.Equal(System.IO.Path.Combine("salida", "edificio-visor-cesium.html"), cesium);
    }
```

- [ ] **Step 2: Run test to verify it fails**

Run: `~/.dotnet/dotnet test tests/LosasPlus.Tests/LosasPlus.Tests.csproj --filter "FullyQualifiedName~GeneradorVisorMapa"`
Expected: FAIL (compilación: `RutasVisores` no existe).

- [ ] **Step 3: Write minimal implementation** (añadir a `GeneradorVisorMapa`)

```csharp
    /// <summary>Rutas de los dos visores junto al .geojson (mismo directorio y base).</summary>
    public static (string MapLibre, string Cesium) RutasVisores(string rutaGeojson)
    {
        string dir = System.IO.Path.GetDirectoryName(rutaGeojson) ?? "";
        string baseNombre = System.IO.Path.GetFileNameWithoutExtension(rutaGeojson);
        return (System.IO.Path.Combine(dir, baseNombre + "-visor-maplibre.html"),
                System.IO.Path.Combine(dir, baseNombre + "-visor-cesium.html"));
    }
```

- [ ] **Step 4: Run test to verify it passes, luego integrar en el ViewModel**

Run: `~/.dotnet/dotnet test tests/LosasPlus.Tests/LosasPlus.Tests.csproj --filter "FullyQualifiedName~GeneradorVisorMapa"`
Expected: PASS (7 tests).

En `MainViewModel.ExportarGeoJson()` (línea ~1098), reemplazar:

```csharp
            System.IO.File.WriteAllText(ruta,
                ExportadorGeoJson.Exportar(edificio, ProyectoActivo.Georreferencia));
            StatusExportacion = $"GeoJSON exportado → {ruta}";
```

por:

```csharp
            var geojson = ExportadorGeoJson.Exportar(edificio, ProyectoActivo.Georreferencia);
            System.IO.File.WriteAllText(ruta, geojson);

            // M.2b: visores 3D autocontenidos junto al .geojson (doble click y abre).
            var (rutaMapLibre, rutaCesium) = GeneradorVisorMapa.RutasVisores(ruta);
            System.IO.File.WriteAllText(rutaMapLibre, GeneradorVisorMapa.GenerarMapLibre(geojson));
            System.IO.File.WriteAllText(rutaCesium, GeneradorVisorMapa.GenerarCesium(geojson));

            StatusExportacion = $"GeoJSON + visores 3D exportados → {ruta}";
```

- [ ] **Step 5: Suite completa + commit**

Run: `~/.dotnet/dotnet test tests/LosasPlus.Tests/LosasPlus.Tests.csproj`
Expected: PASS — 839/839 (832 previos + 7 nuevos), 0 fallos.

```bash
git add src/Core/Services/GeneradorVisorMapa.cs src/MemoriaPlus/ViewModels/MainViewModel.cs tests/LosasPlus.Tests/GeneradorVisorMapaTests.cs
git commit -m "feat(M.2b): export GeoJSON escribe visores MapLibre y Cesium al lado"
```

---

### Task 4: verificación en app real + bitácora BRAIN

**Files:**
- Modify: `~/BRAIN/03_APPS/EstructurasRD/Bitacora de desarrollo.md` (nueva entrada al final)
- Modify: `~/BRAIN/00_MOC/Estado actual.md` (línea de "En foco" de EstructurasRD)

**Interfaces:**
- Consumes: binario MemoriaPlus (proyecto demo georreferenciado con "Ubicar proyecto"), visores generados en Task 3.
- Produces: entrada de bitácora con commits reales; captura opcional `_assets/M2b-visor-maplibre.png`.

- [ ] **Step 1: Verificación manual** — lanzar MemoriaPlus, ubicar el proyecto (botón "Ubicar proyecto"), exportar GeoJSON y abrir `edificio-visor-maplibre.html` en el navegador: edificio extruido visible sobre OSM. Abrir `edificio-visor-cesium.html`: edificio sobre el globo. (Requiere internet; si no hay, documentarlo y seguir.)

- [ ] **Step 2: Bitácora** — añadir entrada `## 2026-07-25 — Fase M.2b: visores 3D MapLibre + Cesium (commits ...)` a la bitácora con el patrón de las entradas existentes (Qué / Idea-decisión / Próximo paso natural: M.3 3D Tiles y contexto urbano). Actualizar "En foco" en `Estado actual.md`.

- [ ] **Step 3: Push de ambos repos**

```bash
git -C ~/dev/estructurasrd/main push origin ui/editor-planta
git -C ~/BRAIN add -A && git -C ~/BRAIN commit -m "EstructurasRD: bitácora M.2b (visores 3D)" && git -C ~/BRAIN push
```
