# UI1.8 — Bordes de continuidad interactivos · Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Llevar la visualización y edición de los bordes de continuidad entre losas al lienzo de Planta 2D (render de hachura por arista + conectores; crear por selección libre de 2 losas; editar/eliminar por menú contextual).

**Architecture:** Servicio de dominio puro `BordesPlantaService` (sin Avalonia, TDD) que concentra la geometría (segmento compartido, eje inferido, hit-test, hachura). `PlantaCanvas` queda como renderer + enrutador de eventos delgado, y `MainViewModel` aloja las mutaciones (con `PushUndoSnapshot`). No se altera el modelo persistido (`BordeAdic`, `BordesX/Y`) ni los formatos `.DL`/`.TXT`.

**Tech Stack:** C# / .NET 8, Avalonia (UI), xUnit (tests). Spec: `docs/superpowers/specs/2026-06-13-ui1.8-bordes-continuidad-design.md`.

**Build/run protocol (obligatorio):**
```bash
export DOTNET_ROOT="$HOME/.dotnet" PATH="$PATH:$HOME/.dotnet"
cd /home/gdc/Downloads/EstructurasRD-engine
dotnet test LosasPlus.Linux.sln                                   # suite completa
dotnet build LosasPlus.Linux.sln --no-incremental -c Release && \
  dotnet run --project src -c Release --no-build                  # app (pase visual)
```

---

## File Structure

- **Create** `src.Core/Services/BordesPlantaService.cs` — servicio puro + tipos de apoyo (`EjeBorde`, `SegmentoBorde`, `AristaHachura`, `BordeLocalizado`). Namespace `LosasPlus.Services`. Una responsabilidad: geometría de bordes de continuidad.
- **Create** `tests/LosasPlus.Tests/Services/BordesPlantaServiceTests.cs` — tests unitarios del servicio. Namespace `LosasPlus.Tests.Services`.
- **Modify** `src/ViewModels/MainViewModel.cs` — `HandleIdClickParaBorde` usa `EjeInferido`; nuevos handlers `ConectarBordesDesdeLienzo`, `AlternarBalanceoBorde`, `EliminarBordeLocalizado`.
- **Modify** `src/Views/PlantaCanvas.cs` — capa de render de bordes en `Render`; rama `ConectarBordes` en `OnPointerPressed` (botón izq.); hit-test de borde en la rama de botón der. (antes del pan) + `ContextMenu`; nuevos eventos.
- **Modify** el code-behind/host donde se suscriben los eventos del `PlantaCanvas` (buscar `CalibracionPdfLista +=`) — cablear los 3 eventos nuevos a los handlers del VM.
- **Modify** markup de toolbar de Planta 2D — botón toggle `🔗 Conectar bordes` que fija `ActiveTool`.

**Convención de coordenadas (modelo, en metros):** una losa es el rectángulo `(CoordenadaX, CoordenadaY)` esquina + extensión `(Lx, Ly)`. Aristas en orden `[N, E, S, W]`: N = `y = CoordenadaY` (superior), S = `y = CoordenadaY+Ly` (inferior), W = `x = CoordenadaX` (izq.), E = `x = CoordenadaX+Lx` (der.). `BordesX` = continuidad según X (cara vertical compartida); `BordesY` = según Y (cara horizontal).

---

## Task 1: `BordesPlantaService.EjeInferido` + tipos de apoyo

**Files:**
- Create: `src.Core/Services/BordesPlantaService.cs`
- Test: `tests/LosasPlus.Tests/Services/BordesPlantaServiceTests.cs`

- [ ] **Step 1: Write the failing test**

Create `tests/LosasPlus.Tests/Services/BordesPlantaServiceTests.cs`:

```csharp
using System.Linq;
using LosasPlus.Models;
using LosasPlus.Services;
using Xunit;

namespace LosasPlus.Tests.Services;

/// <summary>
/// Tests del servicio puro de geometría de bordes de continuidad (UI1.8).
/// </summary>
public class BordesPlantaServiceTests
{
    private static Losa L(int id, double x, double y, double lx = 4, double ly = 3, int tipo = 0)
        => new Losa { Id = id, CoordenadaX = x, CoordenadaY = y, Lx = lx, Ly = ly, Tipo = tipo };

    [Fact]
    public void EjeInferido_lado_a_lado_horizontal_es_X()
    {
        var a = L(1, 0, 0);
        var b = L(2, 4, 0);                       // a la derecha de a
        Assert.Equal(EjeBorde.X, BordesPlantaService.EjeInferido(a, b));
    }

    [Fact]
    public void EjeInferido_apiladas_vertical_es_Y()
    {
        var a = L(1, 0, 0);
        var b = L(2, 0, 3);                        // encima/debajo de a
        Assert.Equal(EjeBorde.Y, BordesPlantaService.EjeInferido(a, b));
    }

    [Fact]
    public void EjeInferido_empate_resuelve_a_X()
    {
        var a = L(1, 0, 0, lx: 2, ly: 2);
        var b = L(2, 2, 2, lx: 2, ly: 2);         // |Δx| == |Δy|
        Assert.Equal(EjeBorde.X, BordesPlantaService.EjeInferido(a, b));
    }
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `dotnet test LosasPlus.Linux.sln --filter "FullyQualifiedName~BordesPlantaServiceTests"`
Expected: FAIL — `BordesPlantaService` / `EjeBorde` no existen (error de compilación).

- [ ] **Step 3: Write minimal implementation**

Create `src.Core/Services/BordesPlantaService.cs`:

```csharp
using System;
using System.Collections.Generic;
using LosasPlus.Models;

namespace LosasPlus.Services;

/// <summary>Eje de continuidad de un borde: X (cara vertical) o Y (cara horizontal).</summary>
public enum EjeBorde { X, Y }

/// <summary>Segmento ancla de un conector de borde, en metros, con su eje.</summary>
public readonly record struct SegmentoBorde(double X0, double Y0, double X1, double Y1, EjeBorde Eje);

/// <summary>Una arista de losa con su tipo visual y coordenadas mundo (metros).</summary>
public readonly record struct AristaHachura(BorderKind Kind, double X0, double Y0, double X1, double Y1);

/// <summary>Un borde localizado por hit-test: el <see cref="BordeAdic"/> y la colección (eje) a la que pertenece.</summary>
public readonly record struct BordeLocalizado(BordeAdic Borde, EjeBorde Eje);

/// <summary>
/// Geometría pura de los bordes de continuidad para Planta 2D (UI1.8). Sin estado,
/// sin I/O, sin Avalonia — testeable en aislamiento.
/// </summary>
public static class BordesPlantaService
{
    /// <summary>Tolerancia de contacto entre caras, en metros.</summary>
    public const double TolContactoM = 0.05;

    /// <summary>
    /// Eje del borde a crear entre dos losas seleccionadas libremente: si la
    /// separación de centroides domina en X las losas van lado a lado (cara ⟂ Lx
    /// ⇒ <see cref="EjeBorde.X"/>); si domina en Y, apiladas (⇒ <see cref="EjeBorde.Y"/>).
    /// Empate resuelto a X (convención del flujo histórico, ahora explícita).
    /// </summary>
    public static EjeBorde EjeInferido(Losa a, Losa b)
    {
        double dx = Math.Abs((a.CoordenadaX + a.Lx / 2) - (b.CoordenadaX + b.Lx / 2));
        double dy = Math.Abs((a.CoordenadaY + a.Ly / 2) - (b.CoordenadaY + b.Ly / 2));
        return dx >= dy ? EjeBorde.X : EjeBorde.Y;
    }
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `dotnet test LosasPlus.Linux.sln --filter "FullyQualifiedName~BordesPlantaServiceTests"`
Expected: PASS (3 tests).

- [ ] **Step 5: Commit**

```bash
git add src.Core/Services/BordesPlantaService.cs tests/LosasPlus.Tests/Services/BordesPlantaServiceTests.cs
git commit -m "feat(bordes): BordesPlantaService.EjeInferido + tipos de apoyo (UI1.8)"
```

---

## Task 2: `BordesPlantaService.SegmentoCompartido`

**Files:**
- Modify: `src.Core/Services/BordesPlantaService.cs`
- Test: `tests/LosasPlus.Tests/Services/BordesPlantaServiceTests.cs`

- [ ] **Step 1: Write the failing test** (añadir estos métodos a la clase de test)

```csharp
    [Fact]
    public void SegmentoCompartido_contacto_pleno_horizontal_devuelve_cara_vertical_eje_X()
    {
        var a = L(1, 0, 0, lx: 4, ly: 3);
        var b = L(2, 4, 0, lx: 4, ly: 3);         // b pegada a la derecha de a
        var seg = BordesPlantaService.SegmentoCompartido(a, b);
        Assert.NotNull(seg);
        Assert.Equal(EjeBorde.X, seg!.Value.Eje);
        Assert.Equal(4, seg.Value.X0, 3);          // cara en x = 4
        Assert.Equal(4, seg.Value.X1, 3);
        Assert.Equal(0, seg.Value.Y0, 3);          // solape y = [0,3]
        Assert.Equal(3, seg.Value.Y1, 3);
    }

    [Fact]
    public void SegmentoCompartido_contacto_pleno_vertical_devuelve_cara_horizontal_eje_Y()
    {
        var a = L(1, 0, 0, lx: 4, ly: 3);
        var b = L(2, 0, 3, lx: 4, ly: 3);         // b pegada encima de a
        var seg = BordesPlantaService.SegmentoCompartido(a, b);
        Assert.NotNull(seg);
        Assert.Equal(EjeBorde.Y, seg!.Value.Eje);
        Assert.Equal(3, seg.Value.Y0, 3);          // cara en y = 3
        Assert.Equal(3, seg.Value.Y1, 3);
    }

    [Fact]
    public void SegmentoCompartido_contacto_parcial_recorta_el_solape()
    {
        var a = L(1, 0, 0, lx: 4, ly: 4);
        var b = L(2, 4, 2, lx: 4, ly: 4);         // desfase vertical: solape y = [2,4]
        var seg = BordesPlantaService.SegmentoCompartido(a, b);
        Assert.NotNull(seg);
        Assert.Equal(EjeBorde.X, seg!.Value.Eje);
        Assert.Equal(2, seg.Value.Y0, 3);
        Assert.Equal(4, seg.Value.Y1, 3);
    }

    [Fact]
    public void SegmentoCompartido_con_holgura_mayor_que_tol_devuelve_null()
    {
        var a = L(1, 0, 0, lx: 4, ly: 3);
        var b = L(2, 5, 0, lx: 4, ly: 3);         // 1 m de hueco
        Assert.Null(BordesPlantaService.SegmentoCompartido(a, b));
    }

    [Fact]
    public void SegmentoCompartido_disjuntas_devuelve_null()
    {
        var a = L(1, 0, 0, lx: 4, ly: 3);
        var b = L(2, 20, 20, lx: 4, ly: 3);
        Assert.Null(BordesPlantaService.SegmentoCompartido(a, b));
    }
```

- [ ] **Step 2: Run test to verify it fails**

Run: `dotnet test LosasPlus.Linux.sln --filter "FullyQualifiedName~BordesPlantaServiceTests"`
Expected: FAIL — `SegmentoCompartido` no existe.

- [ ] **Step 3: Write minimal implementation** (añadir a `BordesPlantaService`)

```csharp
    /// <summary>
    /// Si <paramref name="a"/> y <paramref name="b"/> comparten una cara (hueco ≤
    /// <paramref name="tol"/> y solape &gt; tol en el eje paralelo), devuelve el
    /// segmento de solape en metros y su <see cref="EjeBorde"/>; si no, <c>null</c>.
    /// </summary>
    public static SegmentoBorde? SegmentoCompartido(Losa a, Losa b, double tol = TolContactoM)
    {
        double ax0 = a.CoordenadaX, ax1 = a.CoordenadaX + a.Lx;
        double ay0 = a.CoordenadaY, ay1 = a.CoordenadaY + a.Ly;
        double bx0 = b.CoordenadaX, bx1 = b.CoordenadaX + b.Lx;
        double by0 = b.CoordenadaY, by1 = b.CoordenadaY + b.Ly;

        // Cara vertical compartida (continuidad en X): A.der ~ B.izq  o  B.der ~ A.izq
        bool vertTouch = Math.Abs(ax1 - bx0) <= tol || Math.Abs(bx1 - ax0) <= tol;
        double yLo = Math.Max(ay0, by0), yHi = Math.Min(ay1, by1);
        if (vertTouch && yHi - yLo > tol)
        {
            double x = Math.Abs(ax1 - bx0) <= tol ? (ax1 + bx0) / 2 : (bx1 + ax0) / 2;
            return new SegmentoBorde(x, yLo, x, yHi, EjeBorde.X);
        }

        // Cara horizontal compartida (continuidad en Y): A.inf ~ B.sup  o  B.inf ~ A.sup
        bool horizTouch = Math.Abs(ay1 - by0) <= tol || Math.Abs(by1 - ay0) <= tol;
        double xLo = Math.Max(ax0, bx0), xHi = Math.Min(ax1, bx1);
        if (horizTouch && xHi - xLo > tol)
        {
            double y = Math.Abs(ay1 - by0) <= tol ? (ay1 + by0) / 2 : (by1 + ay0) / 2;
            return new SegmentoBorde(xLo, y, xHi, y, EjeBorde.Y);
        }

        return null;
    }
```

- [ ] **Step 4: Run test to verify it passes**

Run: `dotnet test LosasPlus.Linux.sln --filter "FullyQualifiedName~BordesPlantaServiceTests"`
Expected: PASS (8 tests acumulados).

- [ ] **Step 5: Commit**

```bash
git add src.Core/Services/BordesPlantaService.cs tests/LosasPlus.Tests/Services/BordesPlantaServiceTests.cs
git commit -m "feat(bordes): SegmentoCompartido — cara compartida + eje (UI1.8)"
```

---

## Task 3: `BordesPlantaService.HachuraAristas`

**Files:**
- Modify: `src.Core/Services/BordesPlantaService.cs`
- Test: `tests/LosasPlus.Tests/Services/BordesPlantaServiceTests.cs`

- [ ] **Step 1: Write the failing test**

```csharp
    [Fact]
    public void HachuraAristas_tipo_del_catalogo_mapea_NESW_y_geometria()
    {
        var kvp = TipoLosa.Catalogo.First();       // un tipo válido cualquiera, sin hardcodear el patrón
        var losa = L(1, 1, 2, lx: 4, ly: 3, tipo: kvp.Key);
        var esperado = kvp.Value.Bordes;           // [N,E,S,W]

        var aristas = BordesPlantaService.HachuraAristas(losa);

        Assert.Equal(4, aristas.Count);
        Assert.Equal(esperado[0], aristas[0].Kind); // N
        Assert.Equal(esperado[1], aristas[1].Kind); // E
        Assert.Equal(esperado[2], aristas[2].Kind); // S
        Assert.Equal(esperado[3], aristas[3].Kind); // W
        // N = arista superior (1,2)-(5,2)
        Assert.Equal(1, aristas[0].X0, 3); Assert.Equal(2, aristas[0].Y0, 3);
        Assert.Equal(5, aristas[0].X1, 3); Assert.Equal(2, aristas[0].Y1, 3);
        // W = arista izquierda (1,2)-(1,5)
        Assert.Equal(1, aristas[3].X0, 3); Assert.Equal(2, aristas[3].Y0, 3);
        Assert.Equal(1, aristas[3].X1, 3); Assert.Equal(5, aristas[3].Y1, 3);
    }

    [Fact]
    public void HachuraAristas_tipo_fuera_del_catalogo_degrada_a_4_apoyado()
    {
        int invalido = Enumerable.Range(1, 100000)
            .First(c => !TipoLosa.Catalogo.ContainsKey(TipoLosa.NormalizarCodigo(c)));
        var losa = L(1, 0, 0, tipo: invalido);

        var aristas = BordesPlantaService.HachuraAristas(losa);

        Assert.All(aristas, ar => Assert.Equal(BorderKind.Apoyado, ar.Kind));
    }
```

- [ ] **Step 2: Run test to verify it fails**

Run: `dotnet test LosasPlus.Linux.sln --filter "FullyQualifiedName~BordesPlantaServiceTests"`
Expected: FAIL — `HachuraAristas` no existe.

- [ ] **Step 3: Write minimal implementation** (añadir a `BordesPlantaService`)

```csharp
    private static readonly BorderKind[] CuatroApoyados =
        { BorderKind.Apoyado, BorderKind.Apoyado, BorderKind.Apoyado, BorderKind.Apoyado };

    /// <summary>
    /// Las 4 aristas <c>[N, E, S, W]</c> de la losa con su <see cref="BorderKind"/>
    /// (resuelto desde <c>losa.Tipo</c> en <see cref="TipoLosa.Catalogo"/>) y sus
    /// coordenadas mundo. Tipo fuera del catálogo ⇒ 4 aristas <c>Apoyado</c>.
    /// </summary>
    public static IReadOnlyList<AristaHachura> HachuraAristas(Losa losa)
    {
        var kinds = TipoLosa.Catalogo.TryGetValue(TipoLosa.NormalizarCodigo(losa.Tipo), out var t)
            ? t.Bordes
            : CuatroApoyados;

        double x0 = losa.CoordenadaX, x1 = losa.CoordenadaX + losa.Lx;
        double y0 = losa.CoordenadaY, y1 = losa.CoordenadaY + losa.Ly;
        return new[]
        {
            new AristaHachura(kinds[0], x0, y0, x1, y0), // N — superior
            new AristaHachura(kinds[1], x1, y0, x1, y1), // E — derecha
            new AristaHachura(kinds[2], x0, y1, x1, y1), // S — inferior
            new AristaHachura(kinds[3], x0, y0, x0, y1), // W — izquierda
        };
    }
```

- [ ] **Step 4: Run test to verify it passes**

Run: `dotnet test LosasPlus.Linux.sln --filter "FullyQualifiedName~BordesPlantaServiceTests"`
Expected: PASS (10 tests acumulados).

- [ ] **Step 5: Commit**

```bash
git add src.Core/Services/BordesPlantaService.cs tests/LosasPlus.Tests/Services/BordesPlantaServiceTests.cs
git commit -m "feat(bordes): HachuraAristas — BorderKind por arista NESW (UI1.8)"
```

---

## Task 4: `BordesPlantaService.HitTestBorde`

**Files:**
- Modify: `src.Core/Services/BordesPlantaService.cs`
- Test: `tests/LosasPlus.Tests/Services/BordesPlantaServiceTests.cs`

- [ ] **Step 1: Write the failing test**

```csharp
    private static Sistema SistemaDosLosasConBordeX()
    {
        var s = new Sistema();
        s.Losas.Add(L(1, 0, 0, lx: 4, ly: 3));
        s.Losas.Add(L(2, 4, 0, lx: 4, ly: 3));     // pegada a la derecha
        s.BordesX.Add(new BordeAdic { BI = 1, BJ = 2, Balanceo = "S" });
        return s;
    }

    [Fact]
    public void HitTestBorde_click_sobre_la_cara_compartida_devuelve_el_borde()
    {
        var s = SistemaDosLosasConBordeX();
        var hit = BordesPlantaService.HitTestBorde(4.0, 1.5, s, tol: 0.2);  // sobre x=4, dentro del solape
        Assert.NotNull(hit);
        Assert.Equal(EjeBorde.X, hit!.Value.Eje);
        Assert.Same(s.BordesX[0], hit.Value.Borde);
    }

    [Fact]
    public void HitTestBorde_click_lejano_devuelve_null()
    {
        var s = SistemaDosLosasConBordeX();
        Assert.Null(BordesPlantaService.HitTestBorde(1.0, 1.5, s, tol: 0.2));
    }

    [Fact]
    public void HitTestBorde_borde_con_id_inexistente_se_ignora()
    {
        var s = new Sistema();
        s.Losas.Add(L(1, 0, 0));
        s.BordesX.Add(new BordeAdic { BI = 1, BJ = 99, Balanceo = "S" });  // 99 no existe
        Assert.Null(BordesPlantaService.HitTestBorde(4.0, 1.5, s, tol: 0.5));
    }

    [Fact]
    public void HitTestBorde_sistema_null_devuelve_null()
    {
        Assert.Null(BordesPlantaService.HitTestBorde(0, 0, null!, tol: 0.2));
    }
```

- [ ] **Step 2: Run test to verify it fails**

Run: `dotnet test LosasPlus.Linux.sln --filter "FullyQualifiedName~BordesPlantaServiceTests"`
Expected: FAIL — `HitTestBorde` no existe.

- [ ] **Step 3: Write minimal implementation** (añadir a `BordesPlantaService`)

```csharp
    /// <summary>
    /// Borde de continuidad más cercano al punto <c>(px,py)</c> (metros) dentro de
    /// <paramref name="tol"/>, o <c>null</c>. El ancla de cada borde es su cara
    /// compartida; si las losas no se tocan (par creado libremente), la línea
    /// centroide-a-centroide. Bordes con Ids inexistentes se ignoran.
    /// </summary>
    public static BordeLocalizado? HitTestBorde(double px, double py, Sistema sistema, double tol)
    {
        if (sistema is null) return null;

        BordeLocalizado? mejor = null;
        double mejorDist = double.MaxValue;

        void Probar(IEnumerable<BordeAdic> bordes, EjeBorde ejeColeccion)
        {
            foreach (var borde in bordes)
            {
                var a = BuscarLosa(sistema, borde.BI);
                var b = BuscarLosa(sistema, borde.BJ);
                if (a is null || b is null) continue;
                var seg = SegmentoCompartido(a, b) ?? AnclaCentroides(a, b, ejeColeccion);
                double d = DistanciaPuntoSegmento(px, py, seg.X0, seg.Y0, seg.X1, seg.Y1);
                if (d <= tol && d < mejorDist)
                {
                    mejorDist = d;
                    mejor = new BordeLocalizado(borde, ejeColeccion);
                }
            }
        }

        Probar(sistema.BordesX, EjeBorde.X);
        Probar(sistema.BordesY, EjeBorde.Y);
        return mejor;
    }

    private static Losa? BuscarLosa(Sistema s, int id)
    {
        foreach (var l in s.Losas) if (l.Id == id) return l;
        return null;
    }

    private static SegmentoBorde AnclaCentroides(Losa a, Losa b, EjeBorde eje)
        => new SegmentoBorde(
            a.CoordenadaX + a.Lx / 2, a.CoordenadaY + a.Ly / 2,
            b.CoordenadaX + b.Lx / 2, b.CoordenadaY + b.Ly / 2, eje);

    private static double DistanciaPuntoSegmento(double px, double py, double x0, double y0, double x1, double y1)
    {
        double dx = x1 - x0, dy = y1 - y0;
        double len2 = dx * dx + dy * dy;
        if (len2 < 1e-9) return Math.Sqrt((px - x0) * (px - x0) + (py - y0) * (py - y0));
        double t = Math.Clamp(((px - x0) * dx + (py - y0) * dy) / len2, 0.0, 1.0);
        double cx = x0 + t * dx, cy = y0 + t * dy;
        return Math.Sqrt((px - cx) * (px - cx) + (py - cy) * (py - cy));
    }
```

- [ ] **Step 4: Run test to verify it passes**

Run: `dotnet test LosasPlus.Linux.sln --filter "FullyQualifiedName~BordesPlantaServiceTests"`
Expected: PASS (14 tests acumulados). El servicio puro queda completo.

- [ ] **Step 5: Commit**

```bash
git add src.Core/Services/BordesPlantaService.cs tests/LosasPlus.Tests/Services/BordesPlantaServiceTests.cs
git commit -m "feat(bordes): HitTestBorde — borde más cercano al click (UI1.8)"
```

---

## Task 5: `EjeInferido` en `HandleIdClickParaBorde` (sustituir el hack always-BordesX)

**Files:**
- Modify: `src/ViewModels/MainViewModel.cs:431-443` (cuerpo del segundo click en `HandleIdClickParaBorde`)
- Test: `tests/LosasPlus.Tests/` (suite existente — no debe romperse)

- [ ] **Step 1: Leer el bloque actual** (`HandleIdClickParaBorde`, segundo click):

```csharp
        PushUndoSnapshot();
        // Heurística de eje: si BI<BJ por ID, los agregamos a BordesX (convención
        // simple). El usuario puede después editar el grid si está mal.
        var bi = Math.Min(primer, losaId);
        var bj = Math.Max(primer, losaId);
        var nuevo = new BordeAdic { BI = bi, BJ = bj, Balanceo = "S" };

        // Determinar el balanceo correcto según tipos de las losas.
        if (LosaTieneVoladizo(bi) || LosaTieneVoladizo(bj))
            nuevo.Balanceo = "N";

        SistemaActivo.BordesX.Add(nuevo);
        Log($"Borde X creado: I={bi} J={bj} BAL={nuevo.Balanceo}.");
```

- [ ] **Step 2: Reemplazar por la inferencia de eje**

```csharp
        PushUndoSnapshot();
        var bi = Math.Min(primer, losaId);
        var bj = Math.Max(primer, losaId);
        var nuevo = new BordeAdic { BI = bi, BJ = bj, Balanceo = "S" };

        // Determinar el balanceo correcto según tipos de las losas.
        if (LosaTieneVoladizo(bi) || LosaTieneVoladizo(bj))
            nuevo.Balanceo = "N";

        // Eje inferido por geometría (UI1.8): lado a lado ⇒ BordesX, apiladas ⇒ BordesY.
        var la = SistemaActivo.Losas.FirstOrDefault(l => l.Id == bi);
        var lb = SistemaActivo.Losas.FirstOrDefault(l => l.Id == bj);
        var eje = (la is not null && lb is not null)
            ? BordesPlantaService.EjeInferido(la, lb)
            : EjeBorde.X;
        if (eje == EjeBorde.X) SistemaActivo.BordesX.Add(nuevo);
        else                   SistemaActivo.BordesY.Add(nuevo);
        Log($"Borde {(eje == EjeBorde.X ? "X" : "Y")} creado: I={bi} J={bj} BAL={nuevo.Balanceo}.");
```

Asegurar `using LosasPlus.Services;` y `using System.Linq;` al inicio del archivo (añadir si faltan).

- [ ] **Step 3: Run the full suite to verify no regression**

Run: `dotnet test LosasPlus.Linux.sln`
Expected: PASS — todo verde. Si algún test de `ModoConectarBordes`/`HandleIdClickParaBorde` esperaba `BordesX` para losas **lado a lado**, sigue cayendo en X (sin cambio). Un test que apilara verticalmente y esperara X reflejaba el bug viejo: actualizarlo a `BordesY` y dejar constancia en el mensaje de commit.

- [ ] **Step 4: Commit**

```bash
git add src/ViewModels/MainViewModel.cs
git commit -m "fix(bordes): HandleIdClickParaBorde infiere eje X/Y por geometría (UI1.8)"
```

---

## Task 6: Render de bordes en `PlantaCanvas` (hachura + conectores)

**Files:**
- Modify: `src/Views/PlantaCanvas.cs` — método `Render`, tras el bloque "1. Draw Slabs".

- [ ] **Step 1: Localizar el final del loop de losas** en `Render` (tras `foreach (var sistema in Nivel.Sistemas) { foreach (var losa in sistema.Losas) { … } }`). Inmediatamente después, añadir la capa de bordes (antes de muros/columnas):

```csharp
        // === UI1.8: capa de bordes de continuidad (sobre losas, bajo muros/columnas) ===
        var hachuraPen   = new Pen(new SolidColorBrush(Color.FromRgb(0x2E, 0x7D, 0x32)), 1.5);
        var conectorS    = new Pen(new SolidColorBrush(Color.FromRgb(0x3F, 0x51, 0xB5)), 3.0); // índigo, sólido = Balanceo S
        var conectorN    = new Pen(new SolidColorBrush(Color.FromRgb(0x3F, 0x51, 0xB5)), 3.0)  // discontinuo = Balanceo N
            { DashStyle = new DashStyle(new double[] { 3, 3 }, 0) };

        foreach (var sistema in Nivel.Sistemas)
        {
            // 6a. Hachura por arista (solo lectura, desde losa.Tipo)
            foreach (var losa in sistema.Losas)
            {
                var centroM = new Point(losa.CoordenadaX + losa.Lx / 2, losa.CoordenadaY + losa.Ly / 2);
                foreach (var ar in LosasPlus.Services.BordesPlantaService.HachuraAristas(losa))
                    DibujarHachuraArista(context, hachuraPen, ar, centroM);
            }

            // 6b. Conectores de continuidad (uno por BordeAdic)
            foreach (var b in sistema.BordesX) DibujarConector(context, sistema, b, conectorS, conectorN);
            foreach (var b in sistema.BordesY) DibujarConector(context, sistema, b, conectorS, conectorN);
        }
```

- [ ] **Step 2: Añadir los helpers de dibujo** como métodos privados de `PlantaCanvas` (junto a los demás helpers de render):

```csharp
    private void DibujarHachuraArista(DrawingContext ctx, Pen pen,
        LosasPlus.Services.AristaHachura ar, Point centroM)
    {
        var p0 = MetrosAPixel(ar.X0, ar.Y0);
        var p1 = MetrosAPixel(ar.X1, ar.Y1);
        double largoPx = Math.Sqrt((p1.X - p0.X) * (p1.X - p0.X) + (p1.Y - p0.Y) * (p1.Y - p0.Y));
        if (largoPx < 16) return;  // zoom lejano: no saturar

        switch (ar.Kind)
        {
            case LosasPlus.Models.BorderKind.Empotrado:
            {
                // ticks ⟂ hacia adentro a lo largo de la arista. La normal "hacia
                // adentro" del rectángulo eje-alineado se obtiene apuntando al centro
                // de la losa (centroM, recibido como parámetro desde el loop de Step 1).
                double ux = (p1.X - p0.X) / largoPx, uy = (p1.Y - p0.Y) / largoPx;
                double nx = uy, ny = -ux;                    // normal candidata
                var medio  = new Point((p0.X + p1.X) / 2, (p0.Y + p1.Y) / 2);
                var centro = MetrosAPixel(centroM.X, centroM.Y);
                if ((centro.X - medio.X) * nx + (centro.Y - medio.Y) * ny < 0) { nx = -nx; ny = -ny; }
                double tick = Math.Clamp(largoPx * 0.10, 4, 10);
                for (double d = 8; d < largoPx; d += 10)
                {
                    double bx = p0.X + ux * d, by = p0.Y + uy * d;
                    ctx.DrawLine(pen, new Point(bx, by), new Point(bx + nx * tick, by + ny * tick));
                }
                break;
            }
            case LosasPlus.Models.BorderKind.Libre:
            case LosasPlus.Models.BorderKind.Vuelo:
            {
                var dotted = new Pen(pen.Brush, 1.5) { DashStyle = new DashStyle(new double[] { 2, 2 }, 0) };
                ctx.DrawLine(dotted, p0, p1);
                break;
            }
            // Apoyado: sin añadido (la arista de la losa ya es línea fina)
        }
    }

    private void DibujarConector(DrawingContext ctx, LosasPlus.Models.Sistema sistema,
        LosasPlus.Models.BordeAdic b, Pen penS, Pen penN)
    {
        var a  = sistema.Losas.FirstOrDefault(l => l.Id == b.BI);
        var bb = sistema.Losas.FirstOrDefault(l => l.Id == b.BJ);
        if (a is null || bb is null) return;

        var seg = LosasPlus.Services.BordesPlantaService.SegmentoCompartido(a, bb);
        Point q0, q1;
        if (seg is { } s) { q0 = MetrosAPixel(s.X0, s.Y0); q1 = MetrosAPixel(s.X1, s.Y1); }
        else
        {
            q0 = MetrosAPixel(a.CoordenadaX + a.Lx / 2, a.CoordenadaY + a.Ly / 2);
            q1 = MetrosAPixel(bb.CoordenadaX + bb.Lx / 2, bb.CoordenadaY + bb.Ly / 2);
        }
        ctx.DrawLine(b.Balanceo == "N" ? penN : penS, q0, q1);
    }
```

El centro de la losa (`centroM`) llega como parámetro desde el loop de Step 1, sin estado extra en el control. Añadir `using System.Linq;` si falta en `PlantaCanvas.cs`.

- [ ] **Step 3: Build (no incremental)**

Run: `dotnet build LosasPlus.Linux.sln --no-incremental -c Release 2>&1 | tail -5`
Expected: `Build succeeded. 0 Error(s)`.

- [ ] **Step 4: Pase visual** — `dotnet run --project src -c Release --no-build`. En Planta 2D, abrir un proyecto con losas que tengan tipos empotrados/voladizos y al menos un `BordeAdic`: confirmar ticks ⟂ hacia adentro en aristas empotradas, arista punteada en voladizo/libre, y conector índigo (sólido/discontinuo) entre las losas del borde.

- [ ] **Step 5: Commit**

```bash
git add src/Views/PlantaCanvas.cs
git commit -m "feat(planta): render de hachura por arista + conectores de continuidad (UI1.8)"
```

---

## Task 7: Crear borde por selección libre de 2 losas en el lienzo

**Files:**
- Modify: `src/Views/PlantaCanvas.cs` — campo nuevo + rama `ConectarBordes` en `OnPointerPressed` (botón izq.) + evento.
- Modify: `src/ViewModels/MainViewModel.cs` — handler `ConectarBordesDesdeLienzo`.
- Modify: code-behind/host de eventos del canvas (buscar `CalibracionPdfLista +=`).

- [ ] **Step 1: Campo + evento en `PlantaCanvas`** (junto a los demás campos `_cal*`/eventos):

```csharp
    private int? _primerLosaIdBorde;
    /// <summary>Se dispara con (idA, idB) cuando el usuario elige 2 losas en modo «Conectar bordes».</summary>
    public event Action<int, int>? BordeConexionSolicitada;
```

- [ ] **Step 2: Rama en `OnPointerPressed`**, dentro de `if (pointer.Properties.IsLeftButtonPressed)`, **antes** de la rama `ActiveTool == "Puntero"`:

```csharp
            if (ActiveTool == "ConectarBordes")
            {
                if (HitTest(pM, out _) is Losa losaClic)
                {
                    if (_primerLosaIdBorde is null)
                        _primerLosaIdBorde = losaClic.Id;          // 1er click: recordar
                    else if (_primerLosaIdBorde.Value != losaClic.Id)
                    {
                        BordeConexionSolicitada?.Invoke(_primerLosaIdBorde.Value, losaClic.Id);
                        _primerLosaIdBorde = null;                 // 2º click: crear y resetear
                    }
                    else _primerLosaIdBorde = null;                // misma losa: cancelar
                    InvalidateVisual();
                }
                e.Handled = true;
                return;
            }
```

- [ ] **Step 3: Handler en `MainViewModel`** (junto a `HandleIdClickParaBorde`):

```csharp
    /// <summary>Crea un BordeAdic desde el lienzo (UI1.8): eje inferido + balanceo por voladizo.</summary>
    public void ConectarBordesDesdeLienzo(int idA, int idB)
    {
        if (idA == idB) return;
        var la = SistemaActivo.Losas.FirstOrDefault(l => l.Id == idA);
        var lb = SistemaActivo.Losas.FirstOrDefault(l => l.Id == idB);
        if (la is null || lb is null) return;

        PushUndoSnapshot();
        var bi = Math.Min(idA, idB);
        var bj = Math.Max(idA, idB);
        var nuevo = new BordeAdic { BI = bi, BJ = bj, Balanceo = "S" };
        if (LosaTieneVoladizo(bi) || LosaTieneVoladizo(bj)) nuevo.Balanceo = "N";

        var eje = BordesPlantaService.EjeInferido(la, lb);
        if (eje == EjeBorde.X) SistemaActivo.BordesX.Add(nuevo);
        else                   SistemaActivo.BordesY.Add(nuevo);
        Log($"Borde {(eje == EjeBorde.X ? "X" : "Y")} creado (lienzo): I={bi} J={bj} BAL={nuevo.Balanceo}.");
        RefreshDLContent();
    }
```

- [ ] **Step 4: Cablear el evento** donde se suscriben los demás eventos del `PlantaCanvas` (buscar `CalibracionPdfLista +=` en el code-behind/host; replicar el patrón con la referencia real al canvas y al VM):

```csharp
            plantaCanvas.BordeConexionSolicitada += (idA, idB) => _vm.ConectarBordesDesdeLienzo(idA, idB);
```

- [ ] **Step 5: Build + pase visual**

Run: `dotnet build LosasPlus.Linux.sln --no-incremental -c Release 2>&1 | tail -5` → 0 errores.
App: con `🔗 Conectar bordes` activo (Task 9 cablea el botón), click en 2 losas lado a lado ⇒ conector sólido nuevo; apiladas ⇒ cae en `BordesY`.

- [ ] **Step 6: Commit**

```bash
git add src/Views/PlantaCanvas.cs src/ViewModels/MainViewModel.cs
git commit -m "feat(planta): crear borde por selección libre de 2 losas en el lienzo (UI1.8)"
```

---

## Task 8: Editar/eliminar por menú contextual (click derecho, coexistiendo con el pan)

**Files:**
- Modify: `src/Views/PlantaCanvas.cs` — rama de botón derecho en `OnPointerPressed` + `AbrirMenuBorde` + 2 eventos.
- Modify: `src/ViewModels/MainViewModel.cs` — `AlternarBalanceoBorde`, `EliminarBordeLocalizado`.
- Modify: host de eventos del canvas.

- [ ] **Step 1: Eventos en `PlantaCanvas`:**

```csharp
    public event Action<LosasPlus.Services.BordeLocalizado>? AlternarBalanceoSolicitado;
    public event Action<LosasPlus.Services.BordeLocalizado>? EliminarBordeSolicitado;
```

- [ ] **Step 2: Hit-test de borde ANTES del pan**, en la rama de botón medio/derecho de `OnPointerPressed` (líneas ~342-348). Reemplazar:

```csharp
        if (pointer.Properties.IsMiddleButtonPressed || pointer.Properties.IsRightButtonPressed)
        {
            _isPanning = true;
            e.Pointer.Capture(this);
            e.Handled = true;
            return;
        }
```

por:

```csharp
        if (pointer.Properties.IsMiddleButtonPressed || pointer.Properties.IsRightButtonPressed)
        {
            // UI1.8: click derecho sobre un borde ⇒ menú contextual (no pan).
            if (pointer.Properties.IsRightButtonPressed && Nivel != null)
            {
                var pM = PixelAMetros(pointer.Position);
                foreach (var sistema in Nivel.Sistemas)
                {
                    var hb = LosasPlus.Services.BordesPlantaService.HitTestBorde(pM.X, pM.Y, sistema, 12.0 / _scale);
                    if (hb is { } bl)
                    {
                        AbrirMenuBorde(bl);
                        e.Handled = true;
                        return;
                    }
                }
            }
            _isPanning = true;
            e.Pointer.Capture(this);
            e.Handled = true;
            return;
        }
```

- [ ] **Step 3: `AbrirMenuBorde`** (método privado de `PlantaCanvas`; requiere `using Avalonia.Controls;`):

```csharp
    private void AbrirMenuBorde(LosasPlus.Services.BordeLocalizado bl)
    {
        var menu = new ContextMenu();
        var destino = bl.Borde.Balanceo == "S" ? "N" : "S";
        var toggle = new MenuItem { Header = $"Balanceo: {bl.Borde.Balanceo} → {destino}" };
        toggle.Click += (_, __) => AlternarBalanceoSolicitado?.Invoke(bl);
        var del = new MenuItem { Header = "Eliminar borde" };
        del.Click += (_, __) => EliminarBordeSolicitado?.Invoke(bl);
        menu.Items.Add(toggle);
        menu.Items.Add(del);
        menu.Open(this);
    }
```

- [ ] **Step 4: Handlers en `MainViewModel`:**

```csharp
    public void AlternarBalanceoBorde(BordeLocalizado bl)
    {
        PushUndoSnapshot();
        bl.Borde.Balanceo = bl.Borde.Balanceo == "S" ? "N" : "S";
        Log($"Balanceo del borde I={bl.Borde.BI} J={bl.Borde.BJ} → {bl.Borde.Balanceo}.");
        RefreshDLContent();
    }

    public void EliminarBordeLocalizado(BordeLocalizado bl)
    {
        PushUndoSnapshot();
        var coll = bl.Eje == EjeBorde.X ? SistemaActivo.BordesX : SistemaActivo.BordesY;
        coll.Remove(bl.Borde);
        Log($"Borde eliminado: I={bl.Borde.BI} J={bl.Borde.BJ}.");
        RefreshDLContent();
    }
```

- [ ] **Step 5: Cablear los 2 eventos** junto a `BordeConexionSolicitada`:

```csharp
            plantaCanvas.AlternarBalanceoSolicitado += bl => _vm.AlternarBalanceoBorde(bl);
            plantaCanvas.EliminarBordeSolicitado    += bl => _vm.EliminarBordeLocalizado(bl);
```

- [ ] **Step 6: Build + pase visual**

Run: `dotnet build LosasPlus.Linux.sln --no-incremental -c Release 2>&1 | tail -5` → 0 errores.
App: click derecho sobre un conector ⇒ menú; alternar Balanceo cambia sólido↔punteado; Eliminar lo borra; click derecho en vacío ⇒ sigue haciendo pan. Ctrl+Z revierte cada acción.

- [ ] **Step 7: Commit**

```bash
git add src/Views/PlantaCanvas.cs src/ViewModels/MainViewModel.cs
git commit -m "feat(planta): menú contextual de borde (balanceo/eliminar), coexiste con pan (UI1.8)"
```

---

## Task 9: Botón toggle `🔗 Conectar bordes` + cierre de UI1.8

**Files:**
- Modify: markup de toolbar de Planta 2D (el `.axaml` del shell de Planta 2D; buscar el botón `⛶ Encuadrar` de UI1.7 y añadir el nuevo junto a él).
- Modify: `PLAN_PENDIENTE.md` — marcar UI1.8.

- [ ] **Step 1: Botón toggle** en la toolbar de Planta 2D, fijando `ActiveTool` del canvas. Patrón (ajustar binding al del shell — comando que setea `ActiveTool="ConectarBordes"` / `"Puntero"` al alternar):

```xml
<ToggleButton Content="🔗 Conectar bordes"
              IsChecked="{Binding ModoConectarBordes}"
              ToolTip.Tip="Click en 2 losas para crear un borde de continuidad"/>
```

Conectar `ModoConectarBordes` (bool, ya existe en `MainViewModel`) → cuando es `true`, `PlantaCanvas.ActiveTool = "ConectarBordes"`; cuando es `false`, `"Puntero"`. Si el shell expone `ActiveTool` por binding, enlazar con un convertidor bool→string; si no, manejar en el code-behind al cambiar `ModoConectarBordes`.

- [ ] **Step 2: Build (no incremental)**

Run: `dotnet build LosasPlus.Linux.sln --no-incremental -c Release 2>&1 | tail -5`
Expected: `Build succeeded. 0 Error(s)` (atención a errores AVLN2000 del XAML).

- [ ] **Step 3: Suite completa**

Run: `dotnet test LosasPlus.Linux.sln`
Expected: PASS — verde, con los 14 tests nuevos de `BordesPlantaService`.

Run (motor Python, sin regresión): `( cd motor-fea && .venv/bin/python -m pytest -q )`
Expected: 208 passed.

- [ ] **Step 4: Pase visual completo (aceptación UI1.8)** — recorrer los 5 puntos de la sección 11 del spec:
  1. Hachura: empotrado ⟂ adentro, voladizo punteado, apoyo sin añadido.
  2. `🔗 Conectar bordes` → 2 losas lado a lado ⇒ conector sólido + `BordeAdic` en `BordesX` (verificar en el `.DL`).
  3. 2 losas apiladas ⇒ `BordesY`.
  4. Conectar con un voladizo ⇒ conector discontinuo (Balanceo N).
  5. Click derecho ⇒ menú: alternar Balanceo (sólido↔punteado), Eliminar; Ctrl+Z revierte uno a uno.

- [ ] **Step 5: Cerrar UI1.8 en el tablero** — en `PLAN_PENDIENTE.md`, marcar el ítem **UI1.8** como `[x]` con fecha 2026-06-13 y una línea de cierre (cadena de commits + suite verde), siguiendo el estilo de UI1.6/UI1.7. Estampar `STATE.md` con `estado-real.sh` si el proyecto lo exige para cerrar fase.

- [ ] **Step 6: Commit de cierre**

```bash
git add src/ PLAN_PENDIENTE.md STATE.md
git commit -m "feat(planta): toggle 🔗 Conectar bordes + UI1.8 cerrada (bordes de continuidad interactivos)"
```

---

## Notas de ejecución

- **Suite de referencia:** 1209 .NET verdes al inicio (HEAD `2c0d9cd`); al cerrar deben ser 1209 + 14 = **1223** (si Task 5 no obligó a reescribir tests existentes).
- **GateGuard:** primer `Bash` de la sesión y primer `Edit/Write` de cada archivo rebotan — presentar los hechos pedidos y reintentar idéntico.
- **No push:** `master` vive solo local (origin/main es historia WPF no relacionada).
- **Pure-first:** Tasks 1-4 son 100% testeables y el grueso del valor; si el tiempo aprieta, son un entregable coherente por sí solas (el render/edición se apoya en ellas).
- **Eje del conector vs colección:** `HitTestBorde` devuelve `BordeLocalizado.Eje` = la colección (`BordesX`/`BordesY`) donde vive el borde, que es lo que `EliminarBordeLocalizado` necesita para removerlo del sitio correcto — no la orientación geométrica del segmento ancla.
