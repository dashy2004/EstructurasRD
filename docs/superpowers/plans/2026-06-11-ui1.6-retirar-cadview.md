# UI1.6 — Retirar el modo Plano CAD · Plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Eliminar el modo «🗺 Plano CAD» del shell (CadView/CadCanvasHost/ModoInteraccionCad) dejando Planta 2D como único lienzo, portando antes el panel DXF/PDF, los defaults de muros y la captura para Excel.

**Architecture:** 3 commits, cada uno con build `--no-incremental` 0/0 y suite verde: **A** ports aditivos (el CAD sigue vivo) → **B** borrado del modo y la capa de vista → **C** poda compile-driven del `CadEditorViewModel` + docs. Spec: `docs/superpowers/specs/2026-06-11-ui1.6-retirar-cadview-design.md`.

**Tech Stack:** Avalonia 11 (.NET 8), DirectProperty + SetAndRaise (idioma de PlantaCanvas), RenderTargetBitmap, xUnit.

**Convenciones de esta máquina:** prefijar dotnet con `export DOTNET_ROOT="$HOME/.dotnet" PATH="$PATH:$HOME/.dotnet"`; trabajar desde `/home/gdc/Downloads/EstructurasRD-engine`; GateGuard rebota la primera edición de cada archivo (presentar hechos y reintentar idéntico); Edit exige Read previo del archivo.

---

## Task 1 — Commit A: Ports (PlantaCanvas + panel + captura Excel)

**Files:**
- Modify: `src/Views/PlantaCanvas.cs` (props nuevas tras línea ~102; muro en ~478-485; captura al final de la clase)
- Modify: `src/Views/Planta2DEditorView.axaml` (bindings del canvas :95-99; Expander tras PanelMuro :294)
- Modify: `src/Views/Planta2DEditorView.axaml.cs` (accessor)
- Modify: `src/Views/EditorUnificadoView.axaml` + `.axaml.cs` (x:Name + accessor)
- Modify: `src/ViewModels/MainViewModel.cs` (`ObtenerVistaPlanta2D()` junto a `CurrentView`)
- Modify: `src/MainWindow.axaml.cs` (`OnExportXlsxClick` :378-400)

- [ ] **Step 1.1: PlantaCanvas — tokens de revisión y defaults de muro.** Insertar tras la propiedad `PlanoDxf` (línea ~102), con el idioma DirectProperty del archivo:

```csharp
    // ---- Tokens de revisión (UI1.6): Plano/Pdf son objetos mutables; el VM
    // bumpea RevisionPlano/RevisionPdf al editar Escala/Offset y este canvas
    // redibuja al observarlos — patrón del CadCanvasHost retirado. ----
    private int _revisionPlano;
    public static readonly DirectProperty<PlantaCanvas, int> RevisionPlanoProperty =
        AvaloniaProperty.RegisterDirect<PlantaCanvas, int>(
            nameof(RevisionPlano), o => o.RevisionPlano, (o, v) => o.RevisionPlano = v);
    public int RevisionPlano
    {
        get => _revisionPlano;
        set { if (SetAndRaise(RevisionPlanoProperty, ref _revisionPlano, value)) InvalidateVisual(); }
    }

    private int _revisionPdf;
    public static readonly DirectProperty<PlantaCanvas, int> RevisionPdfProperty =
        AvaloniaProperty.RegisterDirect<PlantaCanvas, int>(
            nameof(RevisionPdf), o => o.RevisionPdf, (o, v) => o.RevisionPdf = v);
    public int RevisionPdf
    {
        get => _revisionPdf;
        set { if (SetAndRaise(RevisionPdfProperty, ref _revisionPdf, value)) InvalidateVisual(); }
    }

    // ---- Defaults de muros nuevos (UI1.6): compartidos con el panel PLANO/PDF
    // vía binding a CadEditor.EspesorMuroNuevo/AlturaMuroNueva (paridad CAD). ----
    private double _espesorMuroNuevo = 0.15;
    public static readonly DirectProperty<PlantaCanvas, double> EspesorMuroNuevoProperty =
        AvaloniaProperty.RegisterDirect<PlantaCanvas, double>(
            nameof(EspesorMuroNuevo), o => o.EspesorMuroNuevo, (o, v) => o.EspesorMuroNuevo = v);
    public double EspesorMuroNuevo
    {
        get => _espesorMuroNuevo;
        set => SetAndRaise(EspesorMuroNuevoProperty, ref _espesorMuroNuevo, value);
    }

    private double _alturaMuroNueva = 3.0;
    public static readonly DirectProperty<PlantaCanvas, double> AlturaMuroNuevaProperty =
        AvaloniaProperty.RegisterDirect<PlantaCanvas, double>(
            nameof(AlturaMuroNueva), o => o.AlturaMuroNueva, (o, v) => o.AlturaMuroNueva = v);
    public double AlturaMuroNueva
    {
        get => _alturaMuroNueva;
        set => SetAndRaise(AlturaMuroNuevaProperty, ref _alturaMuroNueva, value);
    }
```

- [ ] **Step 1.2: PlantaCanvas — el muro nuevo usa los defaults.** En `OnPointerPressed`, herramienta `"Muro"` (líneas ~478-485), cambiar:

```csharp
                        Espesor = 0.15,
                        Altura = 3.0
```
por:
```csharp
                        Espesor = EspesorMuroNuevo,
                        Altura = AlturaMuroNueva
```

- [ ] **Step 1.3: PlantaCanvas — captura PNG.** Añadir al final de la clase (antes del cierre), adaptado de `CadCanvasHost.CaptureCanvasPng` (sin encuadre — viewport actual, mejor esfuerzo):

```csharp
    /// <summary>
    /// Captura la planta como imagen para incrustarla en la exportación a
    /// Excel (UI1.6 — antes se capturaba el lienzo CAD, hoy retirado). Render
    /// del viewport actual; si el control no fue realizado aún (export sin
    /// haber abierto Planta 2D), se mide/arregla a 1200×800 como mejor esfuerzo.
    /// </summary>
    public Avalonia.Media.Imaging.Bitmap CaptureCanvasPng()
    {
        double w = Bounds.Width  >= 1 ? Bounds.Width  : 1200;
        double h = Bounds.Height >= 1 ? Bounds.Height : 800;

        Measure(new Size(w, h));
        Arrange(new Rect(0, 0, w, h));

        var rtb = new Avalonia.Media.Imaging.RenderTargetBitmap(
            new PixelSize((int)Math.Ceiling(w), (int)Math.Ceiling(h)), new Vector(96, 96));
        rtb.Render(this);
        return rtb;
    }
```

- [ ] **Step 1.4: Planta2DEditorView.axaml — bindear las props nuevas.** Reemplazar el elemento `<v:PlantaCanvas …/>` (líneas 95-99) por:

```xml
                    <v:PlantaCanvas x:Name="EditorCanvas" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                                    Nivel="{Binding NivelActivo}" Edificio="{Binding EdificioActivo}"
                                    PdfRef="{Binding CadEditor.Pdf}" FondoPdf="{Binding CadEditor.FondoPdf}"
                                    OpacidadPdf="{Binding CadEditor.OpacidadPdf}"
                                    PlanoDxf="{Binding CadEditor.Plano}"
                                    RevisionPlano="{Binding CadEditor.RevisionPlano}"
                                    RevisionPdf="{Binding CadEditor.RevisionPdf}"
                                    EspesorMuroNuevo="{Binding CadEditor.EspesorMuroNuevo}"
                                    AlturaMuroNueva="{Binding CadEditor.AlturaMuroNueva}"/>
```

- [ ] **Step 1.5: Planta2DEditorView.axaml — panel «PLANO / PDF».** Insertar tras el cierre de `PanelMuro` (`</StackPanel>` de la línea ~294, dentro del StackPanel raíz del panel de propiedades) el Expander portado de `CadView.axaml:333-487`. Cambios deliberados respecto al original: sin botones «Encuadrar» ni «Calibrar» (sin tubería / ya en toolbar), sin grid de espesor/altura del muro seleccionado (PanelMuro ya lo edita — DRY), y los converters ya están en los recursos de esta vista:

```xml
                        <!-- Panel PLANO / PDF (UI1.6): portado del panel lateral del
                             lienzo CAD retirado (CadView.axaml). Mismos bindings
                             CadEditor.*; el lienzo refresca vía RevisionPlano/Pdf. -->
                        <Expander Header="📐 PLANO / PDF" IsExpanded="False">
                            <StackPanel Margin="0,8,0,0">
                                <DockPanel Margin="0,0,0,6" LastChildFill="False">
                                    <TextBlock DockPanel.Dock="Left" Text="PLANO IMPORTADO"
                                               FontWeight="Bold" FontSize="10" VerticalAlignment="Center"
                                               Foreground="{DynamicResource FgMuted}"/>
                                    <Button DockPanel.Dock="Right" Content="🗑" Padding="4,0" FontSize="12"
                                            Background="Transparent" BorderThickness="0" Cursor="Hand"
                                            Foreground="{DynamicResource FgMuted}"
                                            Command="{Binding CadEditor.EliminarDxfCommand}"
                                            ToolTip.Tip="Quitar el plano DXF (no afecta las losas)."/>
                                </DockPanel>
                                <TextBlock Text="{Binding CadEditor.Plano.NombreArchivo, FallbackValue=(sin plano DXF), TargetNullValue=(sin plano DXF)}"
                                           FontFamily="JetBrains Mono, Consolas" FontSize="11"
                                           TextTrimming="CharacterEllipsis"
                                           Foreground="{DynamicResource FgPrimary}"/>
                                <TextBlock FontSize="11" Margin="0,3,0,0" Foreground="{DynamicResource FgMuted}">
                                    <Run Text="{Binding CadEditor.Plano.Ancho, StringFormat=0.0, FallbackValue=0, Mode=OneWay}"/>
                                    <Run Text=" × "/>
                                    <Run Text="{Binding CadEditor.Plano.Alto, StringFormat=0.0, FallbackValue=0, Mode=OneWay}"/>
                                    <Run Text=" m  ·  "/>
                                    <Run Text="{Binding CadEditor.Plano.CantidadEntidades, FallbackValue=0, Mode=OneWay}"/>
                                    <Run Text=" entidades"/>
                                </TextBlock>
                                <Border Height="1" Margin="0,12" Background="{DynamicResource Border}"/>
                                <StackPanel IsEnabled="{Binding CadEditor.TienePlano}">
                                    <TextBlock Text="AJUSTE ESPACIAL" FontWeight="Bold" FontSize="10"
                                               Foreground="{DynamicResource FgMuted}" Margin="0,0,0,8"/>
                                    <TextBlock Text="Escala" FontSize="11" Foreground="{DynamicResource FgSecondary}"/>
                                    <TextBox Margin="0,2,0,8" Text="{Binding CadEditor.EscalaPlano}"/>
                                    <TextBlock Text="Offset X (m)" FontSize="11" Foreground="{DynamicResource FgSecondary}"/>
                                    <TextBox Margin="0,2,0,8" Text="{Binding CadEditor.OffsetXPlano}"/>
                                    <TextBlock Text="Offset Y (m)" FontSize="11" Foreground="{DynamicResource FgSecondary}"/>
                                    <TextBox Margin="0,2,0,12" Text="{Binding CadEditor.OffsetYPlano}"/>
                                </StackPanel>
                                <Border Height="1" Margin="0,12" Background="{DynamicResource Border}"/>
                                <DockPanel Margin="0,0,0,6" LastChildFill="False">
                                    <TextBlock DockPanel.Dock="Left" Text="PDF IMPORTADO"
                                               FontWeight="Bold" FontSize="10" VerticalAlignment="Center"
                                               Foreground="{DynamicResource FgMuted}"/>
                                    <Button DockPanel.Dock="Right" Content="🗑" Padding="4,0" FontSize="12"
                                            Background="Transparent" BorderThickness="0" Cursor="Hand"
                                            Foreground="{DynamicResource FgMuted}"
                                            Command="{Binding CadEditor.EliminarPdfCommand}"
                                            ToolTip.Tip="Quitar el PDF underlay del lienzo."/>
                                </DockPanel>
                                <TextBlock Text="{Binding CadEditor.Pdf.NombreArchivo, FallbackValue=(sin PDF), TargetNullValue=(sin PDF)}"
                                           FontFamily="JetBrains Mono, Consolas" FontSize="11"
                                           TextTrimming="CharacterEllipsis"
                                           Foreground="{DynamicResource FgPrimary}"/>
                                <TextBlock FontSize="11" Margin="0,3,0,0" Foreground="{DynamicResource FgMuted}">
                                    <Run Text="{Binding CadEditor.Pdf.Ancho, StringFormat=0.00, FallbackValue=0, Mode=OneWay}"/>
                                    <Run Text=" × "/>
                                    <Run Text="{Binding CadEditor.Pdf.Alto, StringFormat=0.00, FallbackValue=0, Mode=OneWay}"/>
                                    <Run Text=" m  ·  primera página"/>
                                </TextBlock>
                                <Border Height="1" Margin="0,12" Background="{DynamicResource Border}"/>
                                <StackPanel IsEnabled="{Binding CadEditor.TienePdf}">
                                    <TextBlock Text="AJUSTE ESPACIAL PDF" FontWeight="Bold" FontSize="10"
                                               Foreground="{DynamicResource FgMuted}" Margin="0,0,0,8"/>
                                    <TextBlock Text="Escala" FontSize="11" Foreground="{DynamicResource FgSecondary}"/>
                                    <TextBox Margin="0,2,0,8" Text="{Binding CadEditor.EscalaPdf}"/>
                                    <TextBlock Text="Offset X (m)" FontSize="11" Foreground="{DynamicResource FgSecondary}"/>
                                    <TextBox Margin="0,2,0,8" Text="{Binding CadEditor.OffsetXPdf}"/>
                                    <TextBlock Text="Offset Y (m)" FontSize="11" Foreground="{DynamicResource FgSecondary}"/>
                                    <TextBox Margin="0,2,0,8" Text="{Binding CadEditor.OffsetYPdf}"/>
                                    <TextBlock FontSize="11" Margin="0,4,0,0" Foreground="{DynamicResource FgSecondary}">
                                        <Run Text="Opacidad: "/>
                                        <Run Text="{Binding CadEditor.OpacidadPdf, StringFormat='{}{0:P0}', Mode=OneWay}"/>
                                    </TextBlock>
                                    <Slider Minimum="0" Maximum="1" SmallChange="0.05" LargeChange="0.1"
                                            Value="{Binding CadEditor.OpacidadPdf, Mode=TwoWay}"
                                            TickFrequency="0.1" Margin="0,2,0,8"
                                            ToolTip.Tip="Atenúa el PDF de fondo (0% transparente · 100% opaco)."/>
                                    <CheckBox Content="🌙  Modo Oscuro (Fondo Negro)"
                                              IsChecked="{Binding CadEditor.InvertirColorPdf, Mode=TwoWay}"
                                              Foreground="{DynamicResource FgSecondary}"
                                              Margin="0,4,0,4"
                                              ToolTip.Tip="Invierte los canales del PDF para reducir la fatiga visual."/>
                                </StackPanel>
                                <Border Height="1" Margin="0,12" Background="{DynamicResource Border}"/>
                                <TextBlock Text="🧱  MUROS" FontWeight="Bold" FontSize="10"
                                           Foreground="{DynamicResource FgMuted}" Margin="0,0,0,8"/>
                                <Grid Margin="0,0,0,6" ColumnDefinitions="*,10,*" RowDefinitions="Auto,Auto">
                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Espesor (m)" FontSize="11"
                                               Foreground="{DynamicResource FgSecondary}"/>
                                    <TextBlock Grid.Row="0" Grid.Column="2" Text="Altura (m)" FontSize="11"
                                               Foreground="{DynamicResource FgSecondary}"/>
                                    <TextBox Grid.Row="1" Grid.Column="0" Margin="0,2,0,0"
                                             Text="{Binding CadEditor.EspesorMuroNuevo}"/>
                                    <TextBox Grid.Row="1" Grid.Column="2" Margin="0,2,0,0"
                                             Text="{Binding CadEditor.AlturaMuroNueva}"/>
                                </Grid>
                                <TextBlock FontSize="10" Foreground="{DynamicResource FgMuted}"
                                           TextWrapping="Wrap" Margin="0,0,0,8"
                                           Text="Valores de los muros nuevos («🧱 + Muro» en la toolbar)."/>
                                <ListBox MaxHeight="150" Margin="0,0,0,8"
                                         Background="{DynamicResource BgInput}"
                                         BorderBrush="{DynamicResource Border}" BorderThickness="1"
                                         ItemsSource="{Binding Sistema.Muros}"
                                         SelectedItem="{Binding CadEditor.MuroSeleccionado, Mode=TwoWay}">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <DockPanel>
                                                <Border DockPanel.Dock="Left" Width="11" Height="11"
                                                        CornerRadius="2" Margin="0,0,7,0" VerticalAlignment="Center"
                                                        Background="{Binding Espesor, Converter={StaticResource EspesorABrush}}"/>
                                                <TextBlock FontSize="11" Foreground="{DynamicResource FgPrimary}">
                                                    <Run Text="Muro "/><Run Text="{Binding Id, Mode=OneWay}"/>
                                                    <Run Text="  ·  "/>
                                                    <Run Text="{Binding Longitud, StringFormat=0.00, Mode=OneWay}"/><Run Text=" m"/>
                                                </TextBlock>
                                            </DockPanel>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                                <StackPanel IsEnabled="{Binding CadEditor.MuroSeleccionado, Converter={StaticResource NullToCollapsed}}">
                                    <Button Content="🗑  Eliminar muro" Padding="10,6"
                                            HorizontalAlignment="Stretch"
                                            Command="{Binding CadEditor.EliminarMuroCommand}"
                                            ToolTip.Tip="Quita el muro seleccionado del sistema (Ctrl+Z lo revierte)."/>
                                </StackPanel>
                            </StackPanel>
                        </Expander>
```

- [ ] **Step 1.6: Accessors para la captura.** En `src/Views/Planta2DEditorView.axaml.cs`, tras la línea 22 (`private bool _updatingProperties;`):

```csharp
    /// <summary>Lienzo de planta — lo usa la captura del export a Excel (UI1.6).</summary>
    public PlantaCanvas CanvasPlanta => EditorCanvas;
```

En `src/Views/EditorUnificadoView.axaml` darle nombre al hijo:

```xml
    <v:Planta2DEditorView x:Name="Editor" DataContext="{Binding}"/>
```

En `src/Views/EditorUnificadoView.axaml.cs` (usa `AvaloniaXamlLoader` manual ⇒ sin campos generados; `FindControl`):

```csharp
    /// <summary>Editor de planta hosteado — lo usa la captura del export a Excel (UI1.6).</summary>
    public Planta2DEditorView? Editor => this.FindControl<Planta2DEditorView>("Editor");
```

- [ ] **Step 1.7: MainViewModel — vista de planta bajo demanda.** Junto a `CurrentView`/`CrearVista` (tras el getter de `CurrentView`, ~línea 125):

```csharp
    /// <summary>
    /// Vista del modo Planta 2D, creada y cacheada bajo demanda — la usa el
    /// export a Excel para capturar el lienzo de planta aunque el usuario no
    /// haya visitado el modo (UI1.6). DataContext explícito: creada fuera del
    /// ContentControl no hay herencia.
    /// </summary>
    public Avalonia.Controls.Control? ObtenerVistaPlanta2D()
    {
        if (_vistaCache.TryGetValue(ModoSidebar.Planta2D, out var cacheada)) return cacheada;
        var vista = CrearVista(ModoSidebar.Planta2D);
        if (vista is not null)
        {
            vista.DataContext = this;
            _vistaCache[ModoSidebar.Planta2D] = vista;
        }
        return vista;
    }
```

- [ ] **Step 1.8: MainWindow.axaml.cs — Excel captura la planta.** En `OnExportXlsxClick` (líneas ~383-397), reemplazar el bloque de captura:

```csharp
        // Captura del lienzo de PLANTA como esquema del .xlsx (UI1.6 — antes se
        // capturaba el lienzo CAD; mejor esfuerzo: si la captura falla, se
        // exporta el Excel sin imagen).
        byte[]? png = null;
        try
        {
            if (Vm.ObtenerVistaPlanta2D() is Views.EditorUnificadoView { Editor.CanvasPlanta: { } canvas })
            {
                var bmp = canvas.CaptureCanvasPng();
                using var ms = new MemoryStream();
                bmp.Save(ms);
                png = ms.ToArray();
            }
        }
        catch (Exception ex) { Vm.Log("No se pudo capturar el lienzo de planta: " + ex.Message); }
```

- [ ] **Step 1.9: Build + suite.**

```bash
export DOTNET_ROOT="$HOME/.dotnet" PATH="$PATH:$HOME/.dotnet"
cd /home/gdc/Downloads/EstructurasRD-engine
dotnet build LosasPlus.Linux.sln --no-incremental 2>&1 | tail -4   # 0 Warning(s) / 0 Error(s)
dotnet test LosasPlus.Linux.sln --no-build 2>&1 | tail -3          # Passed! 1216
```

- [ ] **Step 1.10: Commit A.**

```bash
git add src/Views/PlantaCanvas.cs src/Views/Planta2DEditorView.axaml src/Views/Planta2DEditorView.axaml.cs \
        src/Views/EditorUnificadoView.axaml src/Views/EditorUnificadoView.axaml.cs \
        src/ViewModels/MainViewModel.cs src/MainWindow.axaml.cs
git commit -m "feat(planta): panel PLANO/PDF + defaults de muros + captura Excel (UI1.6a)

Ports previos al retiro del modo CAD: Expander con ajuste espacial
DXF/PDF/opacidad/modo-oscuro/muros (bindings CadEditor.* reutilizados),
PlantaCanvas observa RevisionPlano/RevisionPdf (refresco ante mutación
in-place), muros nuevos toman EspesorMuroNuevo/AlturaMuroNueva, y el
export .xlsx captura la planta (CaptureCanvasPng portado).

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

## Task 2 — Commit B: Borrado del modo y la capa de vista CAD

**Files:**
- Modify: `src/MainWindow.axaml` (:5 xmlns, :197-199 radio, :875-878 ContentControl, :390 comentario)
- Modify: `src/ViewModels/MainViewModel.cs` (:60, :83 comentario, :111, :2263-2264 enum)
- Modify: `src/MainWindow.axaml.cs` (:681 array)
- Delete: `src/Views/Cad/CadView.axaml`, `src/Views/Cad/CadView.axaml.cs`, `src/Views/Cad/CadCanvasHost.cs`, `src/Views/Cad/LosaDobleClicEventArgs.cs`, `src/Views/Cad/CalibrarPdfPuntosListosEventArgs.cs`, `src/ViewModels/ModoInteraccionCad.cs`
- Move: `src/Views/Cad/PaletaMuros.cs` → `src/Views/PaletaMuros.cs` (namespace `LosasPlus.Views`)
- Modify: `src/Views/PlantaCanvas.cs` (:916 `Cad.PaletaMuros` → `PaletaMuros`)
- Modify: `src/Views/Planta2DEditorView.axaml` (:4 xmlns cad, :11-12 converters → prefijo `v:`)

- [ ] **Step 2.1: MainWindow.axaml.** Quitar la línea 5 (`xmlns:cad=…`), el RadioButton «🗺 Plano CAD» (3 líneas, :197-199) y el bloque del modo (:875-878):

```xml
                <!-- MODO Lienzo CAD (stub Fase E). -->
                <ContentControl IsVisible="{Binding ModoActivo, Converter={StaticResource EnumToBoolConverter}, ConverterParameter=PlanoCad}">
                    <cad:CadView x:Name="CadEditorView" DataContext="{Binding}"/>
                </ContentControl>
```
También actualizar el comentario-inventario de la línea ~390 («· Inline (IsVisible + EnumToBool): Explorador, Editor, PlanoCad, …» → sin PlanoCad).

- [ ] **Step 2.2: MainViewModel.cs.** (a) `:60`: `if (value is ModoSidebar.Planta2D or ModoSidebar.Vista3D or ModoSidebar.PlanoCad)` → `if (value is ModoSidebar.Planta2D or ModoSidebar.Vista3D)`. (b) `:111`: quitar `or ModoSidebar.PlanoCad` del guard de `CurrentView`. (c) `:83`: del comentario del router, quitar la mención `PlanoCad → CadEditorView/CanvasHost`. (d) `:2263-2264`: quitar del enum la entrada `PlanoCad` y su doc-comment.

- [ ] **Step 2.3: MainWindow.axaml.cs.** Quitar `ModoSidebar.PlanoCad,` del array de `RunVisualVerificationAsync` (:681).

- [ ] **Step 2.4: Borrar archivos y mover la paleta.**

```bash
cd /home/gdc/Downloads/EstructurasRD-engine
git rm src/Views/Cad/CadView.axaml src/Views/Cad/CadView.axaml.cs src/Views/Cad/CadCanvasHost.cs \
       src/Views/Cad/LosaDobleClicEventArgs.cs src/Views/Cad/CalibrarPdfPuntosListosEventArgs.cs \
       src/ViewModels/ModoInteraccionCad.cs
git mv src/Views/Cad/PaletaMuros.cs src/Views/PaletaMuros.cs
```

- [ ] **Step 2.5: PaletaMuros.cs.** `namespace LosasPlus.Views.Cad;` → `namespace LosasPlus.Views;` y en los doc-comments cambiar las menciones `CadView.axaml` → `Planta2DEditorView.axaml` (la leyenda vive ahí desde UI1.4).

- [ ] **Step 2.6: Consumidores de la paleta.** `src/Views/PlantaCanvas.cs:916`: `Cad.PaletaMuros.BrushParaEspesor(...)` → `PaletaMuros.BrushParaEspesor(...)`. `src/Views/Planta2DEditorView.axaml`: quitar `xmlns:cad="clr-namespace:LosasPlus.Views.Cad"` (:4) y en los recursos (:11-12) usar el prefijo `v:` existente:

```xml
        <v:EspesorABrushConverter x:Key="EspesorABrush"/>
        <v:CountToBoolConverter x:Key="CountToBool"/>
```

- [ ] **Step 2.7: Verificar que no queda ninguna referencia.**

```bash
rg -n "CadView|CadCanvasHost|ModoInteraccionCad|PlanoCad|LosaDobleClic|CalibrarPdfPuntosListos" src/ --type-add 'code:*.{cs,axaml}' -tcode
```
Esperado: **0 hits** (los hits de `CadEditorViewModel`/`CadEditor` NO cuentan — ese VM sobrevive; el patrón de arriba no los matchea).

- [ ] **Step 2.8: Build + suite** (mismos comandos del Step 1.9; esperado 0/0 y `Passed! 1216`).

- [ ] **Step 2.9: Commit B.**

```bash
git add -A src/
git commit -m "feat(shell)!: retirar el modo Plano CAD — un solo lienzo (UI1.6b)

Fuera CadView/CadCanvasHost/ModoInteraccionCad y el modo PlanoCad del
sidebar (~2700 líneas). Planta 2D queda como único lienzo: import,
calibración, calcar, muros, resize y panel PLANO/PDF ya viven ahí
(UI1.1-1.5 + UI1.6a). PaletaMuros se muda a Views/ (la usan PlantaCanvas
y la leyenda). Gate: pase visual humano 2026-06-11 ✓.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

## Task 3 — Commit C: Poda del CadEditorViewModel + docs

**Files:**
- Modify: `src/ViewModels/CadEditorViewModel.cs`
- Delete (si la verificación 3.1 da 0 consumidores): `src.Core/Models/Cad/CrearLosaArgs.cs`, `src.Core/Models/Cad/ActualizacionLosaArgs.cs`, `src.Core/Models/Cad/MovimientoGrupoArgs.cs`, `src.Core/Models/Cad/CrearMuroArgs.cs`
- Modify: `PLAN_PENDIENTE.md`, `STATE.md` (vía `./estado-real.sh`)

- [ ] **Step 3.1: Verificar la lista de muertos (post-B).** Cada miembro debe tener 0 hits fuera de `CadEditorViewModel.cs`:

```bash
cd /home/gdc/Downloads/EstructurasRD-engine
for m in ModoInteraccion SnapActivo MoverConectadas SolicitudEncuadre EncuadrarPlanoCommand \
         EncuadrarPdfCommand ReRasterizarPdfCommand CrearLosaCommand ActualizarLosaCommand \
         MoverGrupoCommand CrearBordeAdicCommand CrearMuroCommand MuroSeleccionadoId \
         CatalogoTipos RevisionSistema; do
  echo "== $m"; rg -n "\b$m" src/ tests/ --type-add 'code:*.{cs,axaml}' -tcode | rg -v "ViewModels/CadEditorViewModel.cs"
done
```
Esperado: solo separadores `== …` sin líneas debajo. Si un miembro tiene consumidor vivo (p. ej. un test que construye el comando), **se queda** y se anota en el commit.

- [ ] **Step 3.2: Podar el VM.** Para cada miembro confirmado muerto: quitar la propiedad/comando, su línea de wiring en el constructor (p. ej. `CrearBordeAdicCommand = new RelayCommand(…)` en :56) y su handler privado (`CrearLosa`, `ActualizarLosa`, `MoverGrupo`, `CrearBordeAdic`, `CrearMuro`, `EncuadrarPlano`, `EncuadrarPdf`, `ReRasterizarPdf`). Los `RevisionSistema++` que queden en métodos vivos se eliminan (eran el aviso de redraw del host; Planta redibuja vía `IModeloObservable`). **Conservar**: `RevisionPlano`/`RevisionPdf` (los bindea PlantaCanvas desde A), `ModoCalibrarPdf` + comandos de calibración, `MuroSeleccionado`/`EliminarMuroCommand`, `EspesorMuroNuevo`/`AlturaMuroNueva`, todo el bloque import/mapeo/resumen.

- [ ] **Step 3.3: DTOs huérfanos.**

```bash
for d in CrearLosaArgs ActualizacionLosaArgs MovimientoGrupoArgs CrearMuroArgs CalibracionPdfArgs; do
  echo "== $d"; rg -ln "$d" src/ src.Core/ tests/
done
```
Esperado: los 4 primeros solo en su propio archivo ⇒ `git rm`; `CalibracionPdfArgs` con consumidores (vive). Si `DxfBatchParityTests` u otro test referencia alguno, ese DTO **se queda**.

- [ ] **Step 3.4: Build + suite** (Step 1.9). Esperado `Passed! 1216` — la poda no toca miembros testeados; si un conteo baja, justificar test por test en el commit o revertir el retiro.

- [ ] **Step 3.5: PLAN_PENDIENTE.md.** (a) Línea 33 → cerrada:

```markdown
- [x] **UI1.6** Retirar `CadView`/`CadCanvasHost` del shell (modo PlanoCad). ✅ 2026-06-11 — pase visual humano ✓; modo PlanoCad + vista CAD borrados (~2700 líneas); `CadEditorViewModel` queda como sub-VM de servicios (podado de los miembros host-only); portados a Planta 2D: panel PLANO/PDF (ajuste espacial, opacidad, modo oscuro, muros), defaults de muros nuevos y captura del lienzo para el export .xlsx; `PaletaMuros` → `Views/`. Spec: `docs/superpowers/specs/2026-06-11-ui1.6-retirar-cadview-design.md`.
```
(b) Añadir debajo el hotspot diferido:

```markdown
- [ ] **UI1.7** (hotspot diferido de UI1.6) Render de bordes de continuidad + chips «+» de adyacencia en `PlantaCanvas` — con CAD retirado no hay visualización interactiva de bordes; hoy cubren 🤖 Auto-Conectar y «+ Borde Y/− Borde Y» del modo Editor.
```
(c) En PENDIENTES HUMANOS: marcar el pase visual de UI1 como hecho (✓ 2026-06-11) y añadir:

```markdown
- [ ] **Pase visual corto post-UI1.6**: en Planta 2D, abrir el Expander «📐 PLANO / PDF» — editar Escala/Offset del DXF y del PDF (el lienzo debe refrescar), mover el slider de opacidad, 🗑 quitar y re-importar; crear un muro con espesor custom desde el panel; exportar a Excel y verificar que el .xlsx trae la imagen de la planta.
```

- [ ] **Step 3.6: STATE + commit C.**

```bash
./estado-real.sh                       # re-estampa STATE.md + build.log/test.log
git add -A
git commit -m "refactor(cad-vm): poda host-only del CadEditorViewModel + cierre UI1.6

Fuera los miembros cuyo único consumidor era el CadCanvasHost retirado
(lista cerrada por rg en el plan) y los DTOs de gesto huérfanos de
src.Core/Models/Cad. PLAN_PENDIENTE: UI1.6 cerrada, hotspot UI1.7
(bordes/chips en planta) anotado, pendiente humano corto del panel
nuevo + Excel. STATE re-estampado.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

## Self-review del plan
- **Cobertura del spec:** Commit A ↔ §Commit A (panel, tokens, captura, muro defaults, accessors); Commit B ↔ §Commit B (shell, archivos, PaletaMuros); Commit C ↔ §Commit C (poda, DTOs, PLAN_PENDIENTE/STATE, UI1.7). Exclusiones deliberadas del panel (Encuadrar/Calibrar/grid del muro seleccionado) documentadas en Step 1.5.
- **Sin placeholders:** todos los pasos llevan código/comandos completos; las únicas listas «condicionales» (3.1/3.3) traen el comando de verificación y la regla de decisión.
- **Consistencia de tipos:** `CanvasPlanta` (Step 1.6) = el usado en Step 1.8; `EspesorMuroNuevo/AlturaMuroNueva` coinciden con los nombres existentes del VM (:472/:486); `ObtenerVistaPlanta2D` definido en 1.7 y usado en 1.8.
