# UI1.10 — Muros redimensionables por longitud — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Permitir redimensionar/reorientar un muro arrastrando asas en sus dos extremos en Planta 2D, con un modo "solo longitud" bajo `Shift`.

**Architecture:** Toda la geometría vive en un servicio puro `MurosPlantaService` (TDD, sin UI), al estilo de `BordesPlantaService` (UI1.8). `PlantaCanvas` se cablea con cambios quirúrgicos que **terminan** el patrón de arrastre de extremos ya existente para `Viga`/`EjeEstructural` (`endpointHit`/`_dragEndpoint`). El muro ya es un segmento de dominio (`PuntoInicio`/`PuntoFin` + `Longitud` derivada), así que no cambia el modelo.

**Tech Stack:** C# / .NET, Avalonia (custom-drawn `PlantaCanvas`), xUnit. Build: `dotnet build LosasPlus.Linux.sln --no-incremental -c Release`. Tests: `dotnet test LosasPlus.Linux.sln`.

**Spec:** `docs/superpowers/specs/2026-06-13-ui1.10-muros-redimensionables-design.md`
**Branch:** `engine/ui1.10-muros-redimensionables`

**Environment (cada sesión):**
```bash
export DOTNET_ROOT="$HOME/.dotnet" PATH="$PATH:$HOME/.dotnet"
cd /home/gdc/Downloads/EstructurasRD-engine
```

---

## File Structure

- **Create** `src.Core/Services/MurosPlantaService.cs` — geometría pura: hit-test de asas (`AsaExtremo`), movimiento libre con clamp (`MoverExtremoLibre`), proyección sobre eje con clamp+no-volteo (`ProyectarSobreEje`).
- **Create** `tests/LosasPlus.Tests/Services/MurosPlantaServiceTests.cs` — tests xUnit del servicio.
- **Modify** `src/Views/PlantaCanvas.cs` — `HitTest` (endpoint de muro), `OnPointerPressed` (precheck de asa + captura del extremo), `OnPointerMoved` (rama `Muro` por `_dragEndpoint`), `OnPointerReleased` (reset), `Render` (2 asas + etiqueta de longitud en vivo).

Tasks 1-3 son TDD puro. Tasks 4-6 son glue de vista (sin tests unitarios; se valida en build + gate visual, como UI1.5/1.8).

---

## Task 1: `MurosPlantaService.AsaExtremo` (hit-test de asas)

**Files:**
- Create: `src.Core/Services/MurosPlantaService.cs`
- Test: `tests/LosasPlus.Tests/Services/MurosPlantaServiceTests.cs`

- [ ] **Step 1: Write the failing test**

Create `tests/LosasPlus.Tests/Services/MurosPlantaServiceTests.cs`:

```csharp
using LosasPlus.Models.Cad;
using LosasPlus.Services;
using Xunit;

namespace LosasPlus.Tests.Services;

/// <summary>
/// Tests del servicio puro de geometría de redimensionado de muros (UI1.10).
/// </summary>
public class MurosPlantaServiceTests
{
    private static Muro M(double x0, double y0, double x1, double y1)
        => new Muro { Id = 1, PuntoInicio = new PuntoCad(x0, y0), PuntoFin = new PuntoCad(x1, y1) };

    [Fact]
    public void AsaExtremo_cerca_de_inicio_devuelve_0()
    {
        var m = M(0, 0, 5, 0);
        Assert.Equal(0, MurosPlantaService.AsaExtremo(m, new PuntoM(0.05, 0.02), 0.2));
    }

    [Fact]
    public void AsaExtremo_cerca_de_fin_devuelve_1()
    {
        var m = M(0, 0, 5, 0);
        Assert.Equal(1, MurosPlantaService.AsaExtremo(m, new PuntoM(4.95, 0.0), 0.2));
    }

    [Fact]
    public void AsaExtremo_lejos_devuelve_null()
    {
        var m = M(0, 0, 5, 0);
        Assert.Null(MurosPlantaService.AsaExtremo(m, new PuntoM(2.5, 1.0), 0.2));
    }

    [Fact]
    public void AsaExtremo_empate_gana_el_mas_cercano()
    {
        // Muro corto: ambos extremos dentro de tol; el punto está más cerca de fin.
        var m = M(0, 0, 0.1, 0);
        Assert.Equal(1, MurosPlantaService.AsaExtremo(m, new PuntoM(0.08, 0.0), 0.2));
    }
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `export DOTNET_ROOT="$HOME/.dotnet" PATH="$PATH:$HOME/.dotnet"; cd /home/gdc/Downloads/EstructurasRD-engine && dotnet test LosasPlus.Linux.sln --filter FullyQualifiedName~MurosPlantaServiceTests`
Expected: FAIL — compilación falla (`MurosPlantaService` no existe).

- [ ] **Step 3: Write minimal implementation**

Create `src.Core/Services/MurosPlantaService.cs`:

```csharp
using System;
using LosasPlus.Models.Cad;

namespace LosasPlus.Services;

/// <summary>
/// Geometría pura del redimensionado de muros por asas de extremo (UI1.10). Sin
/// estado, sin I/O, sin Avalonia — testeable en aislamiento. Hermano de
/// <c>BordesPlantaService</c> (UI1.8).
/// </summary>
public static class MurosPlantaService
{
    /// <summary>
    /// Extremo del muro bajo <paramref name="punto"/> (metros): <c>0</c> =
    /// <see cref="Muro.PuntoInicio"/>, <c>1</c> = <see cref="Muro.PuntoFin"/>, o
    /// <c>null</c>. En empate gana el más cercano. <paramref name="tol"/> es el
    /// radio de captura en metros.
    /// </summary>
    public static int? AsaExtremo(Muro muro, PuntoM punto, double tol)
    {
        double di = Dist2(punto.X, punto.Y, muro.PuntoInicio.X, muro.PuntoInicio.Y);
        double df = Dist2(punto.X, punto.Y, muro.PuntoFin.X, muro.PuntoFin.Y);
        double tol2 = tol * tol;
        bool hi = di <= tol2, hf = df <= tol2;
        if (hi && hf) return di <= df ? 0 : 1;
        if (hi) return 0;
        if (hf) return 1;
        return null;
    }

    private static double Dist2(double ax, double ay, double bx, double by)
    {
        double dx = ax - bx, dy = ay - by;
        return dx * dx + dy * dy;
    }
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `dotnet test LosasPlus.Linux.sln --filter FullyQualifiedName~MurosPlantaServiceTests`
Expected: PASS (4 tests).

- [ ] **Step 5: Commit**

```bash
git add src.Core/Services/MurosPlantaService.cs tests/LosasPlus.Tests/Services/MurosPlantaServiceTests.cs
git commit -m "feat(ui1.10): MurosPlantaService.AsaExtremo — hit-test de asas de extremo (TDD)"
```

---

## Task 2: `MurosPlantaService.MoverExtremoLibre` (modo libre con clamp de longitud mínima)

**Files:**
- Modify: `src.Core/Services/MurosPlantaService.cs`
- Test: `tests/LosasPlus.Tests/Services/MurosPlantaServiceTests.cs`

- [ ] **Step 1: Write the failing test**

Append these tests inside `MurosPlantaServiceTests` (antes de la llave de cierre de la clase):

```csharp
    [Fact]
    public void MoverExtremoLibre_punto_normal_es_identidad()
    {
        var fijo = new PuntoCad(0, 0);
        var r = MurosPlantaService.MoverExtremoLibre(fijo, new PuntoM(3, 4), 0.10);
        Assert.Equal(3.0, r.X, 9);
        Assert.Equal(4.0, r.Y, 9);
    }

    [Fact]
    public void MoverExtremoLibre_clampa_a_longitud_minima()
    {
        var fijo = new PuntoCad(0, 0);
        // cursor a 0.04 m sobre +X; minLen 0.10 ⇒ se empuja a (0.10, 0).
        var r = MurosPlantaService.MoverExtremoLibre(fijo, new PuntoM(0.04, 0.0), 0.10);
        Assert.Equal(0.10, r.X, 9);
        Assert.Equal(0.0, r.Y, 9);
    }
```

- [ ] **Step 2: Run test to verify it fails**

Run: `dotnet test LosasPlus.Linux.sln --filter FullyQualifiedName~MurosPlantaServiceTests`
Expected: FAIL — `MoverExtremoLibre` no existe (no compila).

- [ ] **Step 3: Write minimal implementation**

Add this method to `MurosPlantaService` (después de `AsaExtremo`, antes de `Dist2`):

```csharp
    /// <summary>
    /// Modo libre: nueva posición del extremo arrastrado = <paramref name="cursor"/>,
    /// salvo que su distancia a <paramref name="fijo"/> sea menor que
    /// <paramref name="minLen"/>, en cuyo caso se clampa a esa longitud en la
    /// dirección <c>fijo → cursor</c>. Si el cursor coincide con el extremo fijo
    /// (sin dirección), se empuja <paramref name="minLen"/> en +X.
    /// </summary>
    public static PuntoCad MoverExtremoLibre(PuntoCad fijo, PuntoM cursor, double minLen)
    {
        double dx = cursor.X - fijo.X, dy = cursor.Y - fijo.Y;
        double len = Math.Sqrt(dx * dx + dy * dy);
        if (len >= minLen) return new PuntoCad(cursor.X, cursor.Y);
        if (len < 1e-9) return new PuntoCad(fijo.X + minLen, fijo.Y);
        double s = minLen / len;
        return new PuntoCad(fijo.X + dx * s, fijo.Y + dy * s);
    }
```

- [ ] **Step 4: Run test to verify it passes**

Run: `dotnet test LosasPlus.Linux.sln --filter FullyQualifiedName~MurosPlantaServiceTests`
Expected: PASS (6 tests).

- [ ] **Step 5: Commit**

```bash
git add src.Core/Services/MurosPlantaService.cs tests/LosasPlus.Tests/Services/MurosPlantaServiceTests.cs
git commit -m "feat(ui1.10): MurosPlantaService.MoverExtremoLibre — modo libre con clamp de longitud mínima (TDD)"
```

---

## Task 3: `MurosPlantaService.ProyectarSobreEje` (modo Shift: solo longitud, sin volteo)

**Files:**
- Modify: `src.Core/Services/MurosPlantaService.cs`
- Test: `tests/LosasPlus.Tests/Services/MurosPlantaServiceTests.cs`

- [ ] **Step 1: Write the failing test**

Append estos tests dentro de `MurosPlantaServiceTests`:

```csharp
    [Fact]
    public void ProyectarSobreEje_punto_sobre_el_eje_es_identidad()
    {
        var fijo = new PuntoCad(0, 0);
        var refEje = new PuntoCad(5, 0);            // eje horizontal +X
        var r = MurosPlantaService.ProyectarSobreEje(fijo, refEje, new PuntoM(3, 0), 0.10);
        Assert.Equal(3.0, r.X, 9);
        Assert.Equal(0.0, r.Y, 9);
    }

    [Fact]
    public void ProyectarSobreEje_punto_fuera_del_eje_proyecta_perpendicular()
    {
        var fijo = new PuntoCad(0, 0);
        var refEje = new PuntoCad(5, 0);            // eje horizontal
        // cursor (3, 2) ⇒ proyección sobre el eje = (3, 0).
        var r = MurosPlantaService.ProyectarSobreEje(fijo, refEje, new PuntoM(3, 2), 0.10);
        Assert.Equal(3.0, r.X, 9);
        Assert.Equal(0.0, r.Y, 9);
    }

    [Fact]
    public void ProyectarSobreEje_diagonal_mantiene_la_orientacion()
    {
        var fijo = new PuntoCad(0, 0);
        var refEje = new PuntoCad(3, 4);            // eje a 5 m, dir (0.6, 0.8)
        // cursor en (6, 8) está sobre el eje a 10 m ⇒ identidad.
        var r = MurosPlantaService.ProyectarSobreEje(fijo, refEje, new PuntoM(6, 8), 0.10);
        Assert.Equal(6.0, r.X, 9);
        Assert.Equal(8.0, r.Y, 9);
    }

    [Fact]
    public void ProyectarSobreEje_no_voltea_y_clampa_a_longitud_minima()
    {
        var fijo = new PuntoCad(0, 0);
        var refEje = new PuntoCad(5, 0);            // eje +X
        // cursor en el lado opuesto (-3, 0): t < 0 ⇒ clamp a minLen sobre +X.
        var r = MurosPlantaService.ProyectarSobreEje(fijo, refEje, new PuntoM(-3, 0), 0.10);
        Assert.Equal(0.10, r.X, 9);
        Assert.Equal(0.0, r.Y, 9);
    }

    [Fact]
    public void ProyectarSobreEje_eje_degenerado_cae_a_libre()
    {
        var fijo = new PuntoCad(2, 2);
        var refEje = new PuntoCad(2, 2);            // longitud ~0: sin dirección
        var r = MurosPlantaService.ProyectarSobreEje(fijo, refEje, new PuntoM(5, 6), 0.10);
        Assert.Equal(5.0, r.X, 9);
        Assert.Equal(6.0, r.Y, 9);
    }
```

- [ ] **Step 2: Run test to verify it fails**

Run: `dotnet test LosasPlus.Linux.sln --filter FullyQualifiedName~MurosPlantaServiceTests`
Expected: FAIL — `ProyectarSobreEje` no existe (no compila).

- [ ] **Step 3: Write minimal implementation**

Add this method to `MurosPlantaService` (después de `MoverExtremoLibre`):

```csharp
    /// <summary>
    /// Modo Shift (solo longitud): proyecta <paramref name="cursor"/> sobre la recta
    /// que pasa por <paramref name="fijo"/> en la dirección <c>fijo → refEje</c>.
    /// La distancia resultante a <paramref name="fijo"/> se clampa a
    /// <paramref name="minLen"/>, lo que además impide el volteo (la proyección
    /// nunca cae del lado opuesto a <paramref name="refEje"/>). Si el eje es
    /// degenerado (<paramref name="fijo"/> ≈ <paramref name="refEje"/>) cae a
    /// <see cref="MoverExtremoLibre"/>.
    /// </summary>
    public static PuntoCad ProyectarSobreEje(PuntoCad fijo, PuntoCad refEje, PuntoM cursor, double minLen)
    {
        double ax = refEje.X - fijo.X, ay = refEje.Y - fijo.Y;
        double l2 = ax * ax + ay * ay;
        if (l2 < 1e-12) return MoverExtremoLibre(fijo, cursor, minLen);

        double len = Math.Sqrt(l2);
        double ux = ax / len, uy = ay / len;                 // dirección unitaria del eje
        double t = (cursor.X - fijo.X) * ux + (cursor.Y - fijo.Y) * uy;  // proyección escalar (m)
        if (t < minLen) t = minLen;                          // longitud mínima + no-volteo
        return new PuntoCad(fijo.X + ux * t, fijo.Y + uy * t);
    }
```

- [ ] **Step 4: Run test to verify it passes**

Run: `dotnet test LosasPlus.Linux.sln --filter FullyQualifiedName~MurosPlantaServiceTests`
Expected: PASS (11 tests).

- [ ] **Step 5: Commit**

```bash
git add src.Core/Services/MurosPlantaService.cs tests/LosasPlus.Tests/Services/MurosPlantaServiceTests.cs
git commit -m "feat(ui1.10): MurosPlantaService.ProyectarSobreEje — modo Shift solo-longitud sin volteo (TDD)"
```

---

## Task 4: `HitTest` — detectar el extremo del muro (`endpointHit`)

**Files:**
- Modify: `src/Views/PlantaCanvas.cs` (bloque de muros en `HitTest`, ~líneas 788-800)

Sin test unitario: `HitTest` es privado y la cobertura es el build + gate visual. Termina el patrón ya usado por Viga (~776) y Eje (~822).

- [ ] **Step 1: Modify the wall hit-test block**

Reemplazar el bloque de muros dentro de `HitTest`:

```csharp
        // 3. Walls (line segments with thickness) — antes que las losas: un
        // muro pisa la losa de abajo y debe ganar el click/drag (UI1.7).
        foreach (var sistema in Nivel.Sistemas)
        {
            foreach (var muro in sistema.Muros)
            {
                var a = new Point(muro.PuntoInicio.X, muro.PuntoInicio.Y);
                var b = new Point(muro.PuntoFin.X, muro.PuntoFin.Y);
                double dist = DistanceToSegment(p, a, b);
                if (dist <= muro.Espesor / 2.0 + 0.1) // tolerance + half thickness in meters
                {
                    return muro;
                }
            }
        }
```

por:

```csharp
        // 3. Walls (line segments with thickness) — antes que las losas: un
        // muro pisa la losa de abajo y debe ganar el click/drag (UI1.7).
        // Extremos detectados como asas (endpointHit 0/1) — patrón Viga/Eje (UI1.10).
        foreach (var sistema in Nivel.Sistemas)
        {
            foreach (var muro in sistema.Muros)
            {
                var a = new Point(muro.PuntoInicio.X, muro.PuntoInicio.Y);
                var b = new Point(muro.PuntoFin.X, muro.PuntoFin.Y);
                if (DistancePoint(p, a) <= tolE) { endpointHit = 0; return muro; }
                if (DistancePoint(p, b) <= tolE) { endpointHit = 1; return muro; }

                double dist = DistanceToSegment(p, a, b);
                if (dist <= muro.Espesor / 2.0 + 0.1) // tolerance + half thickness in meters
                {
                    return muro;
                }
            }
        }
```

- [ ] **Step 2: Update the press-time Muro capture to use the dragged endpoint**

En `OnPointerPressed`, reemplazar la rama `else if (hit is Muro m)` (~líneas 481-486):

```csharp
                    else if (hit is Muro m)
                    {
                        _dragStartElementX = m.PuntoInicio.X;
                        _dragStartElementY = m.PuntoInicio.Y;
                        // Not capturing PuntoFin for dragging yet, but allows basic selection
                    }
```

por:

```csharp
                    else if (hit is Muro m)
                    {
                        var ext = _dragEndpoint == 1 ? m.PuntoFin : m.PuntoInicio;
                        _dragStartElementX = ext.X;   // posición original del extremo arrastrado (eje Shift)
                        _dragStartElementY = ext.Y;
                    }
```

- [ ] **Step 3: Build**

Run: `export DOTNET_ROOT="$HOME/.dotnet" PATH="$PATH:$HOME/.dotnet"; cd /home/gdc/Downloads/EstructurasRD-engine && dotnet build LosasPlus.Linux.sln --no-incremental -c Release`
Expected: build correcto, 0 errores.

- [ ] **Step 4: Commit**

```bash
git add src/Views/PlantaCanvas.cs
git commit -m "feat(ui1.10): HitTest detecta el extremo del muro (endpointHit) + captura del extremo en press"
```

---

## Task 5: Arrastre del extremo — precheck en press, rama por `_dragEndpoint` en move, reset en release

**Files:**
- Modify: `src/Views/PlantaCanvas.cs` (`OnPointerPressed` precheck, `OnPointerMoved` rama Muro, `OnPointerReleased`)

Sin test unitario (glue de vista); toda la geometría ya está cubierta en Tasks 1-3.

- [ ] **Step 1: Add the asa precheck in OnPointerPressed**

En `OnPointerPressed`, dentro del bloque `if (ActiveTool == "Puntero")`, **justo después** del bloque de precheck de resize de losa (el `if (SelectedElement is Losa losaSel && AsaEnPunto(...))` que termina con `return;`, ~línea 454) y **antes** de `object? hit = HitTest(...)`, insertar:

```csharp
                // Asa de extremo del muro seleccionado (UI1.10) — prioridad sobre
                // el cuerpo del muro; radio de captura generoso como en losa.
                if (SelectedElement is Muro muroSel
                    && MurosPlantaService.AsaExtremo(muroSel, new PuntoM(pM.X, pM.Y), 10.0 / _scale) is { } extMuro)
                {
                    _isDragging = true;
                    _dragEndpoint = extMuro;
                    _dragStartPos = pM;
                    var extPt = extMuro == 0 ? muroSel.PuntoInicio : muroSel.PuntoFin;
                    _dragStartElementX = extPt.X;
                    _dragStartElementY = extPt.Y;
                    GestoEdicionIniciado?.Invoke();   // snapshot de Undo del gesto
                    e.Pointer.Capture(this);
                    e.Handled = true;
                    return;
                }
```

Nota: `MurosPlantaService` y `PuntoM` viven en `LosasPlus.Services`; `PuntoM` ya se usa sin calificar en este archivo (p. ej. el resize de losa). Si el compilador no resuelve `MurosPlantaService`, calificarlo como `LosasPlus.Services.MurosPlantaService` (igual que `LosasPlus.Services.BordesPlantaService` en este mismo archivo).

- [ ] **Step 2: Branch the Muro move by _dragEndpoint in OnPointerMoved**

En `OnPointerMoved`, reemplazar la rama `else if (SelectedElement is Muro m)` (~líneas 700-706):

```csharp
            else if (SelectedElement is Muro m)
            {
                double dxStart = snapResult.X - m.PuntoInicio.X;
                double dyStart = snapResult.Y - m.PuntoInicio.Y;
                m.PuntoInicio = new PuntoCad(m.PuntoInicio.X + dxStart, m.PuntoInicio.Y + dyStart);
                m.PuntoFin = new PuntoCad(m.PuntoFin.X + dxStart, m.PuntoFin.Y + dyStart);
            }
```

por:

```csharp
            else if (SelectedElement is Muro m)
            {
                if (_dragEndpoint == 0 || _dragEndpoint == 1)
                {
                    // Redimensionar por un extremo (UI1.10): el otro extremo queda fijo.
                    var fijo = _dragEndpoint == 0 ? m.PuntoFin : m.PuntoInicio;
                    var refEje = new PuntoCad(_dragStartElementX, _dragStartElementY); // pos. original del extremo
                    var cur = new PuntoM(snapResult.X, snapResult.Y);
                    bool soloLongitud = (e.KeyModifiers & KeyModifiers.Shift) != 0;
                    var nuevo = soloLongitud
                        ? MurosPlantaService.ProyectarSobreEje(fijo, refEje, cur, 0.10)
                        : MurosPlantaService.MoverExtremoLibre(fijo, cur, 0.10);
                    if (_dragEndpoint == 0) m.PuntoInicio = nuevo; else m.PuntoFin = nuevo;
                }
                else
                {
                    // Cuerpo: traslada ambos extremos (comportamiento previo).
                    double dxStart = snapResult.X - m.PuntoInicio.X;
                    double dyStart = snapResult.Y - m.PuntoInicio.Y;
                    m.PuntoInicio = new PuntoCad(m.PuntoInicio.X + dxStart, m.PuntoInicio.Y + dyStart);
                    m.PuntoFin = new PuntoCad(m.PuntoFin.X + dxStart, m.PuntoFin.Y + dyStart);
                }
            }
```

- [ ] **Step 3: Reset _dragEndpoint on release**

En `OnPointerReleased`, añadir el reset junto a los demás (después de `_resizeLosa = null;`):

```csharp
        _resizeLosa = null;   // fin del gesto de resize (UI1.5)
        _dragEndpoint = 2;    // fin del arrastre de extremo (UI1.10)
```

- [ ] **Step 4: Build**

Run: `dotnet build LosasPlus.Linux.sln --no-incremental -c Release`
Expected: build correcto, 0 errores.

- [ ] **Step 5: Commit**

```bash
git add src/Views/PlantaCanvas.cs
git commit -m "feat(ui1.10): arrastre de extremo de muro (libre / Shift solo-longitud) + reset en release"
```

---

## Task 6: `Render` — 2 asas en los extremos + etiqueta de longitud en vivo

**Files:**
- Modify: `src/Views/PlantaCanvas.cs` (loop de muros en `Render`, ~líneas 1044-1062)

- [ ] **Step 1: Add live length label and endpoint handles**

En `Render`, reemplazar el cuerpo del `foreach (var muro in sistema.Muros)` (~líneas 1046-1061):

```csharp
                var pStart = MetrosAPixel(muro.PuntoInicio.X, muro.PuntoInicio.Y);
                var pEnd = MetrosAPixel(muro.PuntoFin.X, muro.PuntoFin.Y);

                bool isSelected = ReferenceEquals(SelectedElement, muro);
                var pen = new Pen(isSelected ? muroSelectBrush : PaletaMuros.BrushParaEspesor(muro.Espesor),
                                  muro.Espesor * _scale)
                {
                    LineCap = PenLineCap.Flat
                };
                
                context.DrawLine(pen, pStart, pEnd);

                // Label
                var ft = new FormattedText($"Muro {muro.Id}", CultureInfo.InvariantCulture, FlowDirection.LeftToRight, boldTypeface, 11.0, Brushes.SaddleBrown);
                var mid = new Point((pStart.X + pEnd.X) / 2.0, (pStart.Y + pEnd.Y) / 2.0 - (muro.Espesor * _scale) / 2.0 - 15.0);
                context.DrawText(ft, mid);
```

por:

```csharp
                var pStart = MetrosAPixel(muro.PuntoInicio.X, muro.PuntoInicio.Y);
                var pEnd = MetrosAPixel(muro.PuntoFin.X, muro.PuntoFin.Y);

                bool isSelected = ReferenceEquals(SelectedElement, muro);
                var pen = new Pen(isSelected ? muroSelectBrush : PaletaMuros.BrushParaEspesor(muro.Espesor),
                                  muro.Espesor * _scale)
                {
                    LineCap = PenLineCap.Flat
                };
                
                context.DrawLine(pen, pStart, pEnd);

                // Label — longitud en vivo cuando está seleccionado (UI1.10).
                string etiqueta = isSelected
                    ? $"Muro {muro.Id} · {muro.Longitud:0.00} m"
                    : $"Muro {muro.Id}";
                var ft = new FormattedText(etiqueta, CultureInfo.InvariantCulture, FlowDirection.LeftToRight, boldTypeface, 11.0, Brushes.SaddleBrown);
                var mid = new Point((pStart.X + pEnd.X) / 2.0, (pStart.Y + pEnd.Y) / 2.0 - (muro.Espesor * _scale) / 2.0 - 15.0);
                context.DrawText(ft, mid);

                // Asas de extremo (UI1.10) — mismo look que las asas de losa (UI1.5).
                if (isSelected)
                {
                    var asaPen = new Pen(Brushes.White, 1.0);
                    context.DrawRectangle(muroSelectBrush, asaPen, new Rect(pStart.X - 4, pStart.Y - 4, 8, 8));
                    context.DrawRectangle(muroSelectBrush, asaPen, new Rect(pEnd.X - 4, pEnd.Y - 4, 8, 8));
                }
```

- [ ] **Step 2: Build**

Run: `dotnet build LosasPlus.Linux.sln --no-incremental -c Release`
Expected: build correcto, 0 errores.

- [ ] **Step 3: Full suite green**

Run: `dotnet test LosasPlus.Linux.sln`
Expected: PASS (1223 previos + 11 nuevos de `MurosPlantaServiceTests` = 1234 .NET).

- [ ] **Step 4: Commit**

```bash
git add src/Views/PlantaCanvas.cs
git commit -m "feat(ui1.10): asas de extremo + etiqueta de longitud en vivo del muro seleccionado"
```

---

## Gate visual (humano, tras Task 6)

Abrir la app y verificar en Planta 2D:
```bash
export DOTNET_ROOT="$HOME/.dotnet" PATH="$PATH:$HOME/.dotnet"
cd /home/gdc/Downloads/EstructurasRD-engine
dotnet build LosasPlus.Linux.sln --no-incremental -c Release && dotnet run --project src -c Release --no-build
```
1. Seleccionar un muro (Puntero) → aparecen **2 asas** en los extremos y la etiqueta muestra `Muro N · L.LL m`.
2. Arrastrar un asa **sin Shift** → el extremo va al cursor (con snap); longitud y ángulo cambian; la lectura sigue en vivo.
3. Arrastrar un asa **con Shift** → el muro solo crece/encoge sobre su eje; la orientación no cambia.
4. Intentar colapsar/voltear → se detiene en ~0.10 m, sin volteo.
5. Arrastrar el **cuerpo** del muro → se mueve entero (como antes).
6. **Ctrl+Z** revierte el gesto completo.
7. El asa gana al cuerpo del muro; el muro sigue ganando a la losa que pisa.

---

## Self-Review

**Spec coverage:**
- Semántica libre/Shift/cuerpo → Tasks 3 (Shift), 2 (libre), 5 (ramificación + cuerpo). ✓
- `AsaExtremo` / `ProyectarSobreEje` / `MoverExtremoLibre` → Tasks 1/3/2. ✓
- minLen 0.10 sin volteo → clamp en Tasks 2 y 3; usado en Task 5. ✓
- Lectura de longitud en vivo → Task 6. ✓
- Snap (libre reusa `PlantaSnapEngine`; Shift snapea y proyecta) → Task 5 usa `snapResult` como cursor en ambos modos. ✓
- Undo por gesto (`GestoEdicionIniciado`) → Task 5 (precheck) y Task 4 Step 2 (vía press path existente, que ya invoca `GestoEdicionIniciado` en ~línea 465). ✓
- Prioridad asa>cuerpo>losa → Task 5 precheck (asa) + Task 4 (endpoint en HitTest, antes del cuerpo y de las losas). ✓
- Eje degenerado → Task 3 (cae a libre). ✓
- Render asas → Task 6. ✓

**Placeholder scan:** sin TBD/TODO; todos los pasos de código muestran el código completo. ✓

**Type consistency:** `AsaExtremo(Muro, PuntoM, double) → int?`, `MoverExtremoLibre(PuntoCad, PuntoM, double) → PuntoCad`, `ProyectarSobreEje(PuntoCad, PuntoCad, PuntoM, double) → PuntoCad` — firmas idénticas en definición (Tasks 1-3) y uso (Task 5). `_dragEndpoint` (0/1/2), `PuntoM(X,Y)`, `PuntoCad(x,y)`, `KeyModifiers.Shift` coinciden con el código existente verificado. ✓
